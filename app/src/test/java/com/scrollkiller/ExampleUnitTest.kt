package com.scrollkiller

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Local unit test — runs on your machine's JVM (fast, no emulator).
 * This trivial test only proves the `test` source set is wired up correctly.
 * Run with: ./gradlew test
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
