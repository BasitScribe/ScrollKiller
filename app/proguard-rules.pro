# ProGuard/R8 rules for release builds (code shrinking & obfuscation).
# Empty for now — minification is disabled in build.gradle.kts until Phase 5.
# When we enable it, keep-rules for reflection-based libraries go here.
