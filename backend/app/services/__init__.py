"""Business logic. Empty in 3a — 3c adds auth, 3d adds sync and the day boundary.

Kept as a package rather than created later so the routers/services/models layout
CLAUDE.md specifies is visible and reviewable in one pass.
"""
