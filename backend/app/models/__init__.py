"""SQLAlchemy models. Empty in 3a — 3b adds every table in docs/SCHEMA.md.

Note for whoever fills this in: importing anything from here into
`app/routers/health.py` fails the build on purpose. See that module's docstring.
"""
