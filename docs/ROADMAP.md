# Roadmap

## Phase 1 — Detection MVP (Android) ✅ COMPLETE
Prove the riskiest 20%. No backend.
- [x] Repo scaffold, Compose app shell
- [x] AccessibilityService: detect Instagram reel swipe (pkg=com.instagram.android), debounce duplicates (activity-reset 200ms, forward-only; PlatformSpec registry)
- [x] Disclosure dialog + onboarding to enable service (disclosure-first → deep-link to Settings → onResume re-check)
- [x] Room: daily count per platform (daily_counts mirror, atomic upsert-increment, survives process death; interim local-date boundary)
- [x] Home screen: live counter + brain state visual (3 states) — live Room-backed counter + degrading mascot (HEALTHY/CRACKING/FRIED at 50/150) on Home AND bubble, one source (BrainState for the state, MascotArt for the art — D36)
- [x] Floating live-counter bubble (SYSTEM_ALERT_WINDOW): draggable mascot+count over tracked apps only, same repository Flow as Home, optional overlay onboarding step (D16); now surface-gated + attached-once, no window churn (D17) — code done; on-device verification pending (HANDOFF)
- [ ] Exit: counter proven accurate vs 50 manual swipes (±2)

## Phase 2 — Block & Challenges ← CURRENT
- [x] **YouTube blocks too, and "5 more minutes" is gone.** Two owner decisions, both reversing an earlier one. **(1) YouTube now covers the screen at its limit (D73), reversing D57.** The calibration evidence D57 refused to promote without — 15 swipes landing 15 ±2, 30s idle moving the count by zero — is *still* missing, so the promotion deliberately does NOT lie about it: `maturity` stays `BETA`, the dashboard keeps its Beta badge, and eligibility comes from a new, separately-named `blocksWhileUncalibrated` flag that `blocksAtLimit` ORs in. That is the split D32 itself prescribed for this exact contingency ("if a Beta platform ever legitimately needs to count toward limits, split the flag deliberately and record why"), chosen over the one-word `maturity = STABLE` edit because that word would have made a false statement in the source AND silently withdrawn the app's own disclosure that the number is unmeasured. **What it risks is bounded**: the block may fire a few Shorts early or late; it cannot land on the wrong screen, because YT is `ENFORCED` on the device-toured `reel_recycler` (D26) and an unmatched marker undercounts. **TikTok and Snapchat remain barred, structurally** — a new test makes the override illegal on a SHADOW platform, since their counts are wrong about *what* they counted, not merely by how much. The YT limit slider appears in Settings with no UI change (it renders per `blocksAtLimit` platform), and `warnEarlyIfBlockIsDead` now covers YT too. **(2) The free "5 more minutes" is deleted (D74)** — button, string, `GRACE_MINUTES`/`GRACE_MS`, the `onSnooze` handler and its constructor parameter, not merely hidden. **Invariant 6 is untouched and was checked first**: a snooze was never an *exit*, it deferred the block and kept the user in the app; Exit and hardware Back are unchanged on all three panels. D50's asserted `CHALLENGE_GRACE > GRACE` inequality died with the thing it constrained (nothing is left to dominate the challenge) and was replaced by non-zero-and-bounded. **Accepted consequence:** on a device that can run no challenge at all, the block panel is Exit and nothing else — a strictly harder block, and still a fully escapable one. **Limiter verified at the unit level** (289 tests green, 5 new): every `blocksAtLimit` platform blocks AT its own limit and not one short of it, on its own count against its own per-platform limit, and every non-blocking platform stays on the bubble at 10,000. **Device verification is still owed** — see HANDOFF. D73/D74
- [x] **Block stabilised: churn, runtime revocation, ReVanced.** Second device capture — the block itself works (rendered full-screen three times), and three defects fell out, **two of them the same bug**. `BlockScreenController.view` was doing double duty as the view handle AND the idempotence guard, so when `addView` failed it stayed null, the guard never armed, and **every count emission inflated a fresh `block_root`** — the repeated teardown in the log. The reason it kept failing is defect two: this MediaTek ROM refuses `SYSTEM_ALERT_WINDOW` at runtime via AppOps **while `Settings.canDrawOverlays()` still returns true**. D51's check fires at the right moment; it was asking a question the ROM answers dishonestly. **The reframing: the authoritative signal is not a permission query, it is whether the window actually exists.** So `show()` now returns `SHOWN/ALREADY_SHOWING/NO_PERMISSION/FAILED/COOLING_DOWN` (callers can no longer discard failure — the same mistake D51 fixed a level up), `view` is assigned only after `isAttachedToWindow` verifies the attach, an `OnAttachStateChangeListener` catches the nastier variant where `addView` succeeds and the ROM tears the window down afterwards, and a pure `BlockRetryPolicy` (30s cooldown, unit-tested against a simulated 480-emission storm → **4 attempts, not 480**) makes repeat triggers genuinely free. `PermissionHealth` gains `overlayRuntimeDenied` — an *observed* input rather than a queried one — with its own `OVERLAY_BLOCKED_BY_SYSTEM` gap and banner copy, because telling a user to grant a permission their settings screen already shows as granted is a dead end. Every hide now names its reason in logcat. **ReVanced:** `PlatformSpec.packageName` → `packageNames: List<String>`; YouTube gains `app.revanced.android.youtube` (same markers, same strategy, same spec, one `daily_counts` row). Raw `ScrollEvent` now records the *actual* client package so a ReVanced Shorts tour is readable. YT stays BETA and ENFORCED, so unverified ReVanced markers can only undercount. **The 17s layout stall is noted, not chased** — churn is the prime suspect; HANDOFF asks for a re-check. D52 — code done; on-device verification pending (HANDOFF)
- [x] **Permission integrity — the app never runs half-dead in silence.** Found by a DEVICE RUN, not a test: what looked like a block regression was the overlay permission revoked by a debug reinstall (`AppOps: Operation not started op=SYSTEM_ALERT_WINDOW`). Detection was perfect throughout — `clips_viewer` MATCH, `counted=true` — the block fired **in code** 100+ times, Android refused the window every time, and **nothing told anyone**. The app counted to 108 and blocked nothing. Root cause: `BlockScreenController.show()` inherited the bubble's fail-soft (`Log.d` and return), which is right for a cosmetic floating pill (D16) and catastrophically wrong for the core promise. Fix is three layers, and the plan states which one is guaranteed: **the Home banner** (needs no permission, no running service — covers every case including "we couldn't even warn you"), **a notification** (best-effort; needs POST_NOTIFICATIONS and a live service), and logcat (developer-only — this is what we had, and it is why 108 happened). New pure `PermissionHealth` + `PermissionGap` is the single source of truth both the block path and the UI read, with `canBlock = accessibility && overlay` — the exact state that caused this (`canDetect` true, `canBlock` false) gets its own named test. Warning fires **early** (once per day, persisted, on entering a reel surface with the block dead — on the failed run that would have been reel 1, not reel 108) and **again at the limit**, sharing one notification id so the second updates the first; `setOnlyAlertOnce` means no per-reel buzz and no rate-limiter state to keep correct; it cancels itself on recovery. Banner shows ONE Fix button routed from the worst gap, two tones (error vs degraded), and refreshes on resume so re-granting self-heals without a restart. POST_NOTIFICATIONS requested from a Settings row (D50's pattern); onboarding stays two steps. **Rejected:** blocking anyway via a full-screen Activity — background activity starts are barred from Android 10 and the `setFullScreenIntent` workaround needs `USE_FULL_SCREEN_INTENT`, auto-denied for non-calling apps at targetSdk 35. D51 — code done; on-device verification pending (HANDOFF, and this one is deliberately reproducible)
- [x] **THE BLOCK IS LIVE ON INSTAGRAM** — the app finally stops you, not just counts you. `blockEnabled = true` on Instagram and **nothing else**: both preconditions the block has been dormant on since D19 are met there and only there — the `clips_viewer` marker is device-TOURED and `ENFORCED` (D26, so the block can only ever land on the reel player, never feed/DMs/profile/Explore/Stories) and the count is CALIBRATED 49/50 (D11), which is what keeps IG `Maturity.STABLE` and therefore makes `blocksAtLimit` true rather than just `blockEnabled`. YouTube/TikTok/Snapchat stay BETA and false on both counts. **Daily limit is the user's**: a per-platform Settings slider (`BlockLimits`: default 100, 20–300 by 10), persisted, read per emission so it is live on the next reel; the slider renders once per `blocksAtLimit` platform, so today exactly one row appears and promoting a platform later needs no UI change. **"5 more minutes"** replaces the old Unlock stub (**itself removed by D74** — the deadline mechanism below survives unchanged and is now written only by a completed challenge): a PERSISTED wall-clock deadline per platform, so the promise survives leaving Instagram and survives the service restarting; no timer schedules the re-block — the render path already compares against the deadline, so the next reel after it lapses blocks and none before it does. **Fixed a bug that would have made the whole feature look broken**: the 3s surface hysteresis fired while the block was up, and since a blocked user cannot scroll, nothing re-armed it — the block appeared and vanished three seconds later, handing the reels straight back. The runnable now re-posts itself while `overlay.isBlocking`. **Always-exitable is now CLAUDE.md invariant 6**, not a comment: Exit and Back both leave from every state, Exit dismisses *before* the launcher intent so a failed intent can't strand anyone, the Back listener stays on the focused root (moving focus to a button would have silently killed Back), and Exit is first in the layout so TalkBack reaches the way out before the way to keep scrolling. Play: the accessibility service description now declares blocking, not just counting. D49 — **VERIFIED ON DEVICE 2026-07-27: all six acceptance runs pass** (Exit works from every state; fires at the limit on Reels; silent on feed/DMs/profile; grace grants then re-blocks; no window churn; count still accurate underneath). Phase 2's core promise is now real on a real phone.
- [x] Overlay block screen at limit (SYSTEM_ALERT_WINDOW) — full-screen block over reels at PlatformSpec.dailyLimit (default 100); Exit/Unlock/Back; dormant until surface gating active (never the feed). D19 — **enabled and reworked by D49** (Unlock → "5 more minutes"; the limit is now the user's, not the spec's), then **"5 more minutes" itself removed by D74** — the reprieve is now earned by a challenge or not had
- [x] Guilt lines carry a `{count}` token, and no-repeat spans a WEEK — two bugs D46's repetition exposed. (1) Lines baked the number into their text, so at 312 scrolls the app said "One-fifty" beside a counter reading 312. Lines now reference the count only via `{count}`/`{minutes}`, substituted on every render so the sentence can never contradict the counter. `GuiltText.offendingNumber` bans digits AND spelled-out scale words AND untokenised time units — a bare-integer check would have caught **zero** of the offenders, since every one was spelled out. Enforced in the parser (drops the line — a bad remote pack degrades) and in a test (fails the build — our own pack must not shrink silently). 27 lines rewritten, 26 tokenised. (2) No-repeat was session-scoped and pool-cycle-based, which at D46's cadence means minutes. It is now a rolling **7-day** window over a persisted `guilt_shown` table (DB v2→v3), read once into memory so the render path stays synchronous. Supersedes D42's daily deck narrowing — exclusion is the hard rule, the daily seed survives only as the fallback tiebreak. Exhaustion falls back to least-recently-shown and **logs the shortfall with the target pool size** rather than papering over it. D47 — code done; on-device verification pending (HANDOFF)
- [x] **The cadence is FLATTENED above 320, and the pool targets are now numbers the build tracks** — D47 named two levers for the content shortfall and deferred both; this takes both. (1) `GuiltCadence.SCHEDULE` loses its 400→4, 500→3, 600→2 and 800→every-scroll rows: it is now 100→every 10, 250→7, **320→5 and 5 forever**, with `FLOOR_SCROLLS = 5` as a named invariant a test enforces at every count. Rows deleted rather than clamped — a row the floor would override lies to the next reader. This is a real product softening at the very top and it buys the content bill back: tier 4 at 800 scrolls/day drops from 196 fires to 117, i.e. from a 1,372-line week to 819. (2) `GuiltPoolMath.EXPANSION_TARGETS` is the committed bill — **T1/T2 18 (met), T3 40, T4 150** — asserted against the engine (40 must exceed T3's permanent ceiling of 35; 150 must cover ~320 scrolls/day) and against the shipped pack, which is pinned as *still short in exactly T3 and T4* so the build stays green today and goes red the day the content lands. The exhaustion log now names both the rate-derived need and the committed target, so "write more" and "this user is past what we chose to cover" stop reading the same. D48
- [ ] **Expand the guilt pack — tier 3 → 40 lines, tier 4 → 150** (currently 18 each). Parallel CONTENT task, owned outside the engine sessions; it does not block anything. Post-D48 consumption: tiers 1–2 fire once/day (18 is ample forever), tier 3 is permanently capped at 5/day (needs 35, so 40 closes it for good — 22 lines and that tier is never discussed again), and tier 4 is unbounded — 126 lines for a 300/day user, 399 at 500, 819 at 800. 150 is a deliberate choice to cover the realistic heavy user (~320/day) and let the least-recently-shown fallback carry the rest, loudly. Community submissions are the long-term tier-4 supply. See D48.
- [x] Guilt lines REPEAT on an escalating cadence — a line no longer fires once per tier crossing and then goes quiet for the rest of the day. `GuiltCadence.SCHEDULE` is one ordered table (100→every 10 scrolls, 250→7, 320→5, 400→4, 500→3, 600→2, 800+→every scroll — **the last four rows are superseded by D48, which plateaus the curve at every 5 from 320**); 50 and 70 keep firing on their tier crossing only, since a repeating nag at 55 reels is the opposite of the design. Frequency is a NEW axis kept separate from intensity — `GuiltTier` still decides which pool, `GuiltCadence` decides how often — so the two are tunable apart and compound at the top. `GuiltFiring` (pure, unit-tested) owns WHEN: baselines on first observation so walking into Reels never fires, re-baselines on a day rollover or a data clear, and holds a gap-suppressed fire PENDING rather than dropping it. Hard ceiling is `MIN_GAP_MS = 5.5s` (4s display + 1.5s counter-only) — the schedule governs eligibility in scrolls, this governs display in seconds, and a shorter gap would mean lines overwrite each other unread and the bubble stops being a counter. D46 — code done; on-device verification pending (HANDOFF)
- [x] Escalating, daily-refreshing guilt lines — lines now ESCALATE with the daily total (`GuiltTier` 50/70/100/150 → intensity 1–4, thresholds named in one `GuiltThresholds` object; **below 50 the app says nothing at all**, which is deliberate), REFRESH day to day from a fixed pack (`GuiltDeck`: 60% of each tier's pool live per day, seeded FNV-1a(day|installId), yesterday's rested lines prioritised back in — no content treadmill, no persistence), and surface EVERYWHERE from ONE pinned draw (`GuiltSelector.current` pins per tier/day/pack/locale, so Home's header, the bubble's threshold nudge and the expanded panel can never say different things). Pack is 72 lines, ~18/tier, India Gen-Z voice, every line tagged `intensity`/`lang`/`region` so region packs are a JSON drop + one Settings entry. NO guilt string exists in Kotlin outside the 3-line crash fallback — enforced by a test that walks the source. `GuiltPackLoader.install()` hot-swap + revision guard intact. D41–D45 — code done; on-device verification pending (HANDOFF)
- [x] guilt_pack.json loader, weighted no-repeat rotation — bundled assets/guilt_pack.json in remote-config shape (schema_version/revision/pack_id/locale, per-line id+category+weight, `surfaces` tone map); 4 categories (roast/existential/reverse_psych/pride, D9); GuiltRotation = weighted draw, no repeat until the pool cycles, no back-to-back across a cycle reset or across surfaces; GuiltPackLoader.install() is the hot-swap seam for a Phase-3 server pack. Block screen + bubble (brain-state escalation nudge) both pull from it. D33 — code done; on-device verification pending
- [x] **Challenge engine + "Walk 20 steps" end to end** — the block no longer has only a free tap past it. `ChallengeSpec {id, type, target, sensorStrategy, promptRes}` + `ChallengeRegistry`, shaped on `PlatformSpec` deliberately: adding jump/face-down/forehead is a spec entry plus a sensor strategy, not a rewrite. `type` (what you do) and `sensorStrategy` (how we measure) are split for the same reason `PlatformSpec` splits `platform` from `advanceStrategy` — WALK alone already needs two measurement paths. `ACCEL_PEAKS`/`ORIENTATION_HOLD` ship declared-and-unused like `AdvanceStrategy.EVENT_PULSE`, and `ChallengeRegistry.IMPLEMENTED` states the gap honestly with a test asserting no enabled spec falls outside it. **The reward is deliberately unequal**: a completed challenge grants **15 minutes vs the tap's 5** — equal rewards would make the challenge strictly dominated and the feature dead on arrival, and a test asserts the inequality so a tuning edit can't reintroduce it. Sensor: `TYPE_STEP_DETECTOR` preferred (incremental, low latency), `TYPE_STEP_COUNTER` as the runtime fallback with baselining and reboot-reset handling; registered with `maxReportLatencyUs = 0` because a batched flush makes the ring sit still then jump, which reads as broken. **Invariant 6 held and shaped the design**: the challenge panel carries its OWN Exit — a challenge screen with only "Back" would be a second screen to escape before you can escape — and it is a child swap inside the existing block window, so no new window and no rebuild. Unavailable sensor or permission → the button simply isn't there; discovery moves to a Settings row, the one place a runtime permission can actually be requested (an AccessibilityService has no Activity). D50 — code done; on-device verification pending (HANDOFF)
- [x] **"Jump 10 times" (`ACCEL_PEAKS`) + the challenge chooser** — the block's second physical unlock, and the first time the user picks. Jump detection counts a free-fall→landing **sequence**, not a peak: a landing spike alone would let someone clear it by shaking the phone on the sofa, so free fall arms the detector and only an armed spike counts (a test pins 20 un-armed spikes scoring zero). Guarded by a 400ms refractory (one landing is a spike plus decaying oscillation — otherwise one jump counts four times), a 1s arm expiry (human airtime is 200–500ms), and per-device gravity baselining, so every threshold is a fraction of *this* sensor's resting magnitude rather than an absolute m/s². `SENSOR_DELAY_GAME` because a spike is tens of ms wide and `_UI` can sample either side and miss it. **Selection moved to the user** (agency here is accessibility — you can't jump at 2am in a flat, can't walk seated on a train): a third `chooser_panel` in the same window, rows built from availability rather than declared in XML, plus "Surprise me" that never repeats the previous draw. Panel toggling centralised so "exactly one visible" can't be broken at five call sites. **Invariant 6 grew with it** — the chooser is a panel you can be on, so it carries its own Exit, listed first for TalkBack. **Availability is now per-spec**: the step sensors are the only ones behind a runtime permission, so the old app-wide question was hiding a jump challenge that needs no permission from anyone who declined one. Reward stays flat at 15 min for every challenge (per-spec pricing would re-punish the user who picked walking *because* they can't jump). D53 — code done; on-device verification pending (HANDOFF)
- [x] **"Phone face down 30s" (`ORIENTATION_HOLD`)** — the first challenge measured in TIME, and it opened three things. **Breaking RESETS to zero, never pauses**: a pause would make it six five-second flips with a peek between each, so `HoldDetector` forgets the hold entirely and a test pins that five separate five-second holds total nothing. The rule is in the prompt copy, because learning it by losing 25 seconds reads as broken rather than strict. Progress arrives as ONE absolute `onHoldElapsed(seconds)` mirroring `onCumulative`, so the reset falls out of the semantics and needs no second signal — which **invalidated the old "completion is sticky" guarantee** (holds are deliberately non-monotonic; `ChallengeController`'s latch already makes `onComplete` fire once, and a test now pins the non-stickiness so nobody clamps it back). **The screen timeout would have killed this silently** — a default timeout is 30s, exactly the hold length, so the display sleeps at the moment of truth and the accelerometer stops: fixed with `FLAG_KEEP_SCREEN_ON` scoped to the challenge panel and cleared on every path off it (a screen held awake by a torn-down challenge is the same bug class as a leaked sensor). **Haptics replace the invisible ring**: 400ms buzz on completion, two-pulse on a break — different shapes, not lengths, so "done" can't be confused with "you lost it" through a cushion. `VIBRATE` is a normal install-time permission; no vibrator → silence, everything else intact. Ring label counts DOWN in seconds while the arc still fills. `SENSOR_DELAY_NORMAL` not jump's `_GAME` — a hold has no transient to catch. D54 — code done; on-device verification pending (HANDOFF)
- [x] **"Forehead 30s" (`PROXIMITY_HOLD`) — the challenge suite is COMPLETE (4/4)** — proximity NEAR **and** device upright, because covered alone is a thumb on a table: a two-second cheat on a thirty-second task. NEAR is `value < sensor.maximumRange`, not a hard-coded `< 5f`, because many devices report only binary 0/max rather than real centimetres. First source to register **two sensors**, both on ONE listener so a single `unregisterListener` releases both and `stop()` cannot half-release; a partial registration is cleaned up before returning false. **`HoldDetector` reused completely unchanged** — same 30s, same reset-on-break, same haptics, same `KEEP_SCREEN_ON`, same seconds ring, same flat reward — so the increment was one new file and four small edits. That reuse is the payoff for having kept the detector pure. Only strategy whose hardware check can genuinely fail on a shipped device (proximity is near-universal but not guaranteed) → first real exercise of graceful absence. `IMPLEMENTED` now equals the full declared set for the first time, which a test states explicitly along with what to do when the next strategy is declared ahead of its code. D55 — code done; on-device verification pending (HANDOFF)
- [ ] Fake-scroll feed challenge — the one remaining mechanic from the original challenge line. NOT a sensor challenge (a decoy feed that scrolls but shows nothing), so it needs none of the `ChallengeSensorSource` machinery and is deliberately deferred rather than forced into it.
- [x] YT Shorts + TikTok + Snapchat detection — all three ship BETA (D32): they count and display, but `Maturity.BETA` makes them structurally ineligible to drive a limit or block (`blocksAtLimit = blockEnabled && STABLE`). Instagram is the only STABLE platform. TikTok/Snapchat were never toured (Snapchat overcounts). YT's advance signal is now RESOLVED (D34): the capture proved `TYPE_VIEW_SCROLLED` is dead on Shorts (deltaY=0 always) and the real per-Short signal is an identity on `TYPE_WINDOW_CONTENT_CHANGED`, so YT runs `AdvanceStrategy.IDENTITY_CHANGE` — implemented, unit-tested, and was **v1-DECIDED as BETA (D57): YouTube counts but never blocks** — the two acceptance runs (15 swipes → 15 ±2; 30s idle → no movement) were requested across four sessions and never produced, and promotion was REFUSED on absent evidence rather than granted on elapsed time. **↳ SUPERSEDED BY D73 (2026-07-28): YouTube DOES block.** The evidence is still missing and D57's reasoning about it still stands; what changed is the weighing — an unenforced doomscrolling surface is its own product failure. So YT blocks via the explicit `blocksWhileUncalibrated` override while staying BETA and Beta-badged, rather than by claiming a calibration it does not have. TikTok/Snapchat remain barred (SHADOW). The STABLE promotion path is still the one-line enum flip whenever the capture exists — and it should delete the override in the same commit. Previously: SCAFFOLDED + gated (registry specs, ENFORCED w/ candidate markers so they can't overcount). D23/D24
- [x] Surface discrimination (only Reels/Shorts count) — GatingMode {PASSTHROUGH/SHADOW/ENFORCED}. TOURED 2026-07-24: IG marker LOCKED to "clips_viewer" + flipped to ENFORCED (feed/profile/DMs/Stories all NO_MATCH); YouTube fixed (support-lib RecyclerView container mismatch — D27) + ENFORCED on proven "reel_recycler". Overlay surface state moved to per-event + 3s hysteresis (window-scan false-positived on YT feed's Shorts shelf — D28). TikTok + Snapchat left SHADOW (not toured). blockEnabled still false everywhere. D26–D29. Pending on-device: ±2/50 recalibration under ENFORCED + YT direction re-tour.
- [x] Raw scroll_events log + 7d prune (invariant #4/D4); DB v2 migration. D20
- [x] Bubble visibility — surface-driven (visible on reel surface, hides on leave via 3s hysteresis); entry-count credits the landing reel, EXCEPT on an IDENTITY_CHANGE platform where the identity path already counts it (D34). D29 (supersedes D21's idle-dismiss)
- [x] Bubble content — shows the GRAND TOTAL across all platforms (was the per-app count); brain state derives from the total, so the bubble and Home agree. D35's `IG 30 · YT 10` subtitle line is superseded by the tap-expand panel below. D35
- [x] Bubble tap-expand panel — a tap toggles a breakdown panel open/closed IN PLACE (it used to launch Home — wrong for an overlay): today's total + estimated time, then one proportional bar per platform with a count above zero, longest first. Same window, root stays VISIBLE and only a CHILD toggles, so it is a surface resize and D30's no-churn property holds. Bars derive from the same `TodaySummary` emission as the headline total (new pure `BubbleBreakdown`), so they can't disagree. View tree extracted to `BubbleView`. D37 — code done; on-device verification pending (HANDOFF)
- [x] Dashboard UI — Material3 bottom-nav tabs (Today/Apps/Settings), Compose; time-estimate, per-app bar chart, clear-data, bubble toggle. D22
- [x] Mascot art — the emoji brain is replaced by a real character (4 poses: HEALTHY/CRACKING/FRIED + block-screen-only GUARDIAN) on Home's hero, the bubble, and the block screen. One mapping in `MascotArt`; masters in `art/mascot/`, imported to pre-scaled mdpi…xxxhdpi drawables by `tools/mascot_import.py` (two size families — 120dp hero, 40dp bubble — so the overlay never runtime-scales). Thresholds unchanged. D36
- [x] Mascot fills the bubble — the D36 import shipped 3 of 4 masters opaque, which meant `union_trim()` had never run and the 40dp bitmap was a full square canvas with the character at ~70% of it. The importer now keys the backgrounds itself (border-seeded geodesic flood on smoothness, NOT a colour key — see D37 for why that distinction is what makes it safe), the shared union trim runs (100%→71.7%x87.4% of canvas), and output is emitted at the crop's natural aspect (33x40dp bubble, 98x120dp hero) instead of being forced square. `HERO_DP`/`BUBBLE_DP` now mean HEIGHT; consumers bind height and let width follow. Character fill at true 40dp went 73%→84% of the mascot box in height and 68%→95% in width. D37 — code done; on-device verification pending (HANDOFF)
- [x] Expanded panel stays on screen — D37's panel grew off the display whenever the pill had been dragged near an edge, because the window carries `FLAG_LAYOUT_NO_LIMITS` (deliberately — it is what lets the pill sit flush against an edge) so the system never pulls it back as it grows. The window now contains itself: pure `OverlayPlacement` (COMPACT clamps the drag position; EXPANDED centres horizontally, keeps the pill's y, clamps the bottom) over a usable rect from `OverlayMetrics` (WindowMetrics + insets-ignoring-visibility on API 30+, real display minus framework bar dimens on 26–29), recomputed on expand/collapse/drag/rotation. Gravity stays TOP|START in both shapes; compact drag position tracked separately from `params.x/y` so collapsing returns the pill where the user left it. Still ONE window — every path is `updateViewLayout`, skipped when x/y already match. D38 — code done; on-device verification pending (HANDOFF Runs 1 & 4)
- [x] Count updates on detection, not on the DB round trip — `record()` bumps an in-memory `PendingCounts` synchronously before dispatching the write, and every read Flow merges it with Room via `max` (pure `TodayMerge`: running totals, not deltas, so the count is monotonic and converges as writes land). Room is still the source of truth and the only durable store. `clearAll()` resets pending after the deletes; a day rollover drops it by date. All reads are now projections of `observeTodaySummary()`, so no surface can miss the optimistic path (D35 restated). DEBUG-only `CountLatency` prints one `LATENCY` line per advance attributing DETECT→OPTIMISTIC→PERSISTED→EMITTED→RENDERED. No debounce/threshold/gating change. D39 — code done; **the timing measurement itself is NOT done** (device was PIN-locked) and is HANDOFF Run 2
- [x] Platform icons in the panel — the `IG`/`YT` text labels became monochrome glyphs via a new `PlatformSpec.iconRes`, tinted to the same recessive white, in a column the same width so the bars still line up. Glyphs are deliberately GENERIC (Material Icons, Apache 2.0 — camera / video player / music note / flash bolt), not the platforms' brand marks: the panel draws inside those apps' own UI and the bar already carries the count. Licensed marks are a drop-in at `res/drawable/ic_platform_*.xml` with no code change. D40 — code done; on-device verification pending (HANDOFF Run 3)

## Phase 3 — Backend & Accounts

**DESIGNED 2026-07-28, not started.** Sequenced into five sub-phases, one per session. The spine of
the sequence: **3a–3d do not touch `app/` at all**, so the shipped offline app cannot regress while
the backend is built; 3e is the only client change and is inert when signed out. The app must keep
working with zero backend, forever — sync is purely additive.

- [x] **3a — CI + skeleton + security baseline. The pipeline is the deliverable, not a means to one (D60).** SHIPPED 2026-07-28 — see the status log for what the first CI run actually found. `backend/` scaffold (monorepo, D59), ruff/mypy/pytest, hash-pinned deps, multi-stage **non-root digest-pinned** Dockerfile, `/health` (no DB) vs `/readyz` (DB), Trivy + syft SBOM + gitleaks, **first-ever `.github/workflows/`** with path filters (android.yml + backend.yml). CI covers **both** trees: the Android build has never once run in CI, so its reproducibility on a clean machine is unproven. Secrets handling from day one. Security exists before the first endpoint.
- [ ] **3b — data layer + migrations.** SQLAlchemy 2.x async models for every SCHEMA.md table; `platform` as a Postgres ENUM with a **parity test against the client's `Platform.id` values** (a mismatch is a silent sync drop); `users.timezone` validated as IANA; Alembic on the **direct** URL, app on the **pooled** one with prepared statements disabled (D62). `devices`/`friendships` created unused — Phase 4's consumers.
- [ ] **3c — auth, hardened from the start.** Google id_token verified against JWKS (iss/aud/exp); own short-TTL access JWT + **rotating refresh with reuse detection → family revocation** (D63); refresh hashed at rest; rate limits; no tokens or PII in logs.
- [ ] **3d — sync + /me/today.** `POST /sync`: `INSERT sync_batches ON CONFLICT DO NOTHING`, upsert `daily_counts += delta` only if inserted, ack batch_ids. Never absolute counts (invariant 1). Server-side day boundary from `users.timezone` (D64, invariant 2), DST-tested. 7-day prune (invariant 4) via scheduled Action, not an always-on worker.
- [ ] **3e — client sync queue** (the only session touching `app/`). Room **v4** additive `sync_queue`; `CountRepository.record()` also enqueues; flush is **event-driven, not periodic** (see below); ack-gated dequeue per D65; **signed out = zero network, zero behaviour change**.

**The sync trigger is three events, not a timer.** WorkManager's *periodic* minimum is 15 minutes, so
the original spec's "flush every 60s" was never achievable and is dropped rather than rounded up. The
real mechanism: **(1)** flush on app-background (`ON_STOP` — the moment a batch is most likely to be
complete and the user is least likely to notice work); **(2)** while the user is actively scrolling, a
**chained one-shot** `OneTimeWorkRequest` that re-enqueues itself with a delay, giving sub-15-minute
cadence without a periodic worker and stopping cleanly when there is nothing queued; **(3)** flush on
**connectivity change**, via a `NetworkType.CONNECTED` constraint so a queue drained by a dead network
goes out the instant one returns. Backoff is exponential on failure. None of the three can lose a
delta, because dequeue is gated on the server's ack (D65), not on the request being sent.

**Cold starts are a design input, not a footnote.** Render free spins down (~15 min idle) and Neon
free autosuspends, so the first request after idle can take seconds. Three rules, baked into the
sub-phases that implement them: `/sync` is **idempotent and therefore spin-up-agnostic** — a timeout
is retried with the same `batch_id` and the server dedupes (3d); `/me/today` is **fire-and-forget with
a short timeout and must never block a UI frame** — Home renders the local number first and the server
total folds in when it arrives, never the other way round (3e); `/health` **must not touch the DB**
(3a) or a suspended database reads as a dead app and the platform restarts a healthy service.
*(D61 reserved — written in the sub-phase that measures the real spin-up, not guessed now.)*

**Reconciliation is settled: D65.** `displayed = server_acked_total + local_unacked_deltas`. The
server owns acked history and the *date boundary*; the device owns observations not yet credited.
Nothing is overwritten — a delta migrates from local-unacked to server-baseline on ack, never
double-counted, never dropped. CRDT-shaped (server total is a G-Counter, deltas are increments), which
is *why* invariant 1 forbids absolute-count POSTs. Hangs off the existing `PendingCounts`/`TodayMerge`
layer as a third input. Signed out, `server_acked_total` is 0 and today's behaviour falls out unchanged.

**Redis + FCM are NOT Phase 3 (D66).** ZSETs are keyed by `group_id` and friend-groups do not exist
until Phase 4, so they would be infrastructure with no consumer; the Upstash free tier is also
command-metered. Invariant 3 is a caching rule, not a mandate to use Redis now. The line: **schema may
lead its consumer, services may not** — which is why `devices`/`friendships` *are* created (unused) in
3b, while a Redis client is not.

## Phase 4 — Social
- [ ] Friendships, battle groups, Upstash ZSET leaderboards, 30s cache
- [ ] FCM: rank-change taunts
- [ ] Streaks + "beat yesterday"

## Phase 5 — Ship
- [ ] GitHub Releases APK, closed testing (20 testers/14d), Play listing ($25)
- [ ] **Store copy must clear docs/STORE_COPY.md before submission** — a checklist of claims the app is not allowed to make, started at D49. First entry: never market the block as unbypassable.
- [ ] iOS skeleton in Actions macOS runner (build-green only)

## Status log
- (append: date — phase — what shipped / what broke)
- 2026-07-29 — Phase 2 — **Pass B1: the Insights data layer, no UI (D80/D81).** Split from the screen deliberately so a chart bug could never leave a half-migrated DB behind. **(1) RANGE QUERIES** — every `DailyCountDao` query was `WHERE date = :date`, so history was stored (D4) and unreachable. Added three inclusive-range queries (daily totals, per-(date,platform) rows, per-platform totals over a range); `BETWEEN` is safe because ISO-8601 sorts lexicographically as it sorts chronologically, which is why D14 chose it. Ranges are parameters, not three hardcoded 7/30 queries — one statement, no SQL to drift. **(2) SESSION ROLLUP + DB v4.** Sessions need timestamps; `scroll_events` is pruned at 7d and invariant 4 is not a tuning knob, so extending retention was rejected outright — buying a nicer chart with a permanent timestamped log of when someone is on their phone is the trade a privacy-pitched product least gets to make. Instead `maybePrune` became `rollUpAndPrune`: distil each day's sessions to one integer in the new `daily_minutes` table, THEN prune. Ordering is the whole design. SUBTLE CORE: the prune cutoff is ROLLING, so an old day bleeds events through the day — re-rolling it would overwrite a complete figure with a partial one and the number would silently SHRINK. So past days use INSERT-OR-IGNORE (first write wins, and the first write for a finished day is complete) while today uses REPLACE (still accumulating, each pass dominates the last). Sessions split on 5-min silence and sum spans — first-to-last across a day would count the eight hours between a morning and an evening scroll AS scrolling. Stored as integer SECONDS, not REAL minutes, so summing 30 rows is exact. New table not a `daily_counts` column, which mirrors the server shape (D14). **(3) STREAKS** — pure, clock-free, `today`/`from` as parameters. Under = `total < limit` matching BlockPolicy (`<=` would call the day you were blocked a success); a MISSING day counts as under (zero scrolls is not a failure); today is judged now, not at midnight; future-dated rows ignored. **FLAGGED PROVISIONAL (D81):** streaks are entirely a claim about which day a count belongs to, and D14's boundary is INTERIM — Phase 3's server timezone truth can shift a best streak. Consequence recorded now because it constrains Pass C: milestones must DERIVE from the calculator, never cache a badge that later becomes arithmetically false. **(4) NEW `MigrationSqlTest`** asserts every hand-written CREATE against Room's exported schema JSON — a mismatch throws on the UPGRADE launch, on a user's device, and cannot fail on a fresh install. Both prior migrations claimed in comments to match; the claim was true and unenforced. Mutation-checked: deleting one space fails it. **(5) THE RAMP, accepted** — rollups only exist from ship-day forward, so time history shows a ~1-month boundary; `observeEarliestRolledDate()` exists so B2 renders it rather than blending two different measurements into one series. 334 unit tests green (+26). NOT BUILT: the Insights screen — next session.
- 2026-07-29 — Phase 3a — **Container scan fixed: it was a dependency, not the base image.** Diagnosed by RUNNING Trivy locally rather than waiting on the CI table (same move that corrected the gitleaks inference). Base image `python:3.13-slim-bookworm@sha256:9d7f28…` scans **0 vulnerabilities** — no digest re-pin needed. The finding is `starlette==0.48.0` with three fixable HIGH CVEs: CVE-2025-62727 (Range-header DoS, fixed 0.49.1), CVE-2026-48818 (SSRF/NTLM theft via UNC paths in StaticFiles, fixed 1.1.0) and CVE-2026-54283 (form() limits silently ignored, fixed 1.3.1) — so 1.3.1 is the floor that clears all three. starlette is TRANSITIVE: `requirements.in` declares only fastapi/pydantic-settings/uvicorn, and `fastapi==0.118.0` capped it. A resolver probe (not a guess) picked `fastapi==0.140.13` as the version that admits starlette 1.3.1. TRAP WORTH RECORDING: bumping fastapi and recompiling was NOT enough — pip-compile reuses an existing pin while it stays satisfiable, and fastapi 0.140.13's range still permits 0.48.0, so the first regenerated lockfile silently kept the vulnerable version. Only `--upgrade-package starlette` moved it. Verified: all four backend gates green (ruff check, ruff format, mypy, pytest at 100% coverage) and the Trivy fs scan now reports 0. Note two `test_repo_layout` tests fail if only `backend/` is mounted — they assert on repo-ROOT files; that is a mount artifact, and they pass with the full repo.
- 2026-07-29 — Phase 3a — **android/build fixed: the daemon JVM pin drops its vendor (D79, correcting D67).** The first CI run died before any task ran: `Unable to download toolchain matching the requirements ({languageVersion=21, vendor=JetBrains, ...}) from 'null'... No defined toolchain download url for LINUX on x86_64`. TWO WRONG GUESSES were eliminated by the log rather than by argument — it was neither a simple Temurin/JBR swap nor a foojay provisioning failure. `foojay-resolver-convention` resolves ORDINARY toolchains; the daemon criteria goes through `DaemonJavaToolchainProvisioningService`, which reads only explicit `toolchainUrl.<OS>.<ARCH>` entries, of which there were none — hence a download URL of literally `null`. THE REAL FINDING is that D67 was internally contradictory: it said CI runs Temurin AND that the vendor pin must never be relaxed, but a vendor-pinned daemon REQUIRES a JBR, so no workflow-only change could satisfy it. Its instruction to a successor was unfollowable, and was written with no CI run to check it against. The vendor line was never chosen either — the file opens `#This file is generated by updateDaemonJvm`, which captures whatever JVM was current, so a machine-specific accident got preserved as a decision and then defended by an ADR built on top of it. FIX: drop `toolchainVendor`, keep `toolchainVersion=21` — which RESTORES D67's stated intent ("local and CI... different JVMs of the same version") rather than relaxing it. Rejected: installing JBR in CI (honours the letter, entrenches an accidental pin, and costs 30-60s of D68's minute budget every run since JBR is not in the hosted toolcache), and hardcoding a `toolchainUrl` (rots every JBR release, changes local resolution too). Neither buys anything, because nothing about building needs a specific vendor. Verified: `gradlew --version` reports `Compatible with Java 21, any vendor` with the launcher still on JBR locally; CI's exact command minus `lintDebug` is green (308 tests, assembleDebug, compileReleaseKotlin). `lintDebug` still fails LOCALLY on the machine-specific `local.properties` PropertyEscape error the workflow header already documents as unable to fire on a runner. STILL OPEN: the backend container scan — its Trivy CVE rows have not come through, and base-digest-stale vs dependency-pin have different fixes.
- 2026-07-29 — Phase 3a — **First CI run triaged: secret scan fixed, android + container held for logs.** The gitleaks job passed on `push` and failed on `pull_request` — an asymmetry that is diagnosable from config alone: a push scans only that push's commits, while the PR scans the whole branch diff against main, so a finding in `465e937` (the 3a baseline commit) was never re-scanned by the recent Android pushes. VERIFIED RATHER THAN INFERRED, using the container escape hatch documented in security.yml's own header: 18 commits scanned, exactly ONE finding — `generic-api-key` on `backend/tests/test_logging_redaction.py:50`, the fixture `"rt_live_abc123"`. Worth recording that the first inference was WRONG — reasoning from the repo alone pointed at the fake JWT on line 70, and the allowlist written for it would not have fixed anything. Running the scanner is what caught that. FALSE POSITIVE, nothing to rotate: `_live_` is the shape real providers use for production keys so the rule was doing its job, but `abc123` is a placeholder, it authenticates against nothing (this service has no auth until 3b/3c — which is why the scanner went in first, D60), and redaction is keyed on the FIELD NAME, so the fixture's realism is incidental to the assertion. New `.gitleaks.toml` scopes the exemption to that one literal rather than to `tests/`, because fixtures are among the likeliest places for a real credential to land by accident. `[extend] useDefault = true` is load-bearing — a custom config REPLACES the default ruleset, so without it the scan would go green by no longer looking, which is worse than no scanner. Both directions were then proven: history scan clean, and a planted non-example AWS key still caught. (The first canary used AWS's documentation key, which gitleaks allowlists by default — a bad canary that would have produced a false all-clear.) HELD, deliberately: the android and container failures have no logs yet. Android is NOT the simple JBR swap first assumed — `settings.gradle.kts` configures foojay-resolver-convention 0.10.0 and Gradle 9.5 can auto-provision the daemon JVM through it, so there are now two candidate causes with different fixes and guessing would churn the workflow. Container scan runs `ignore-unfixed: true`, so its failure means a FIXABLE HIGH/CRITICAL — most likely the digest-pinned `python:3.13-slim-bookworm` base going stale, but it could equally be the filesystem scan on a requirements pin, and the two have different remedies.
- 2026-07-29 — Phase 2 — **Appeal pass A: the app stops opening on a permission request (D78).** Researching the wellbeing category surfaced a defect nothing in the codebase would have: first run was Disclosure → Accessibility → Overlay → Dashboard, so a new user's FIRST screen was a request to enable an Accessibility Service before the app had shown them anything of value. The flow was correct and unpersuasive, which is the kind of bug only a category comparison finds. NEW `WelcomeScreen` in front of the disclosure: healthy mascot, one question, a real zero, one button. **INVARIANT 5 INTACT** — the welcome REQUESTS nothing; Play requires the disclosure before *enabling*, not before the app's first pixel. Because that is a line a future edit could cross unnoticed, the route moved out of `MainActivity`'s untested `when` into pure `OnboardingRoute.stepFor(...)`, and `OnboardingRouteTest` pins that the disclosure is unreachable-past while accessibility is off (exhaustively), that welcome hands off to it and never past it, and that the route is total over all 16 inputs. The `!welcomeSeen && !accessibilityEnabled` gate is the non-obvious half: gating on the flag alone would shove an introduction at an existing user upgrading, whose pref is false only because the key did not exist. COPY claims nothing about the user — the first draft's "You scroll about 2 hours a day" is a statement about someone we have not measured, which STORE_COPY forbids for in-app copy too (same trap D77 caught in the service description); the preview shows a REAL zero rather than a staged demo. **Brand quiet tier** (`ON_LIGHT_MUTED`/`ON_LIGHT_FAINT`, cobalt-biased): every colour was identity or state, so a divider or gridline had nowhere to come from — the light-side twins of the existing `ON_DARK_*` pair were simply missing. **Empty states** on Today's breakdown and the whole Apps tab, with the tone rule that nothing counted is this app's BEST outcome, so they must not look like failure. ALSO: STORE_COPY still listed "two sanctioned ways past" the block naming "5 more minutes" — stale since D77, and a compliance defect rather than a doc one since that file is the pre-submission gate. 308 unit tests green (+8 OnboardingRouteTest). PENDING on-device: HANDOFF Runs K (first run + the upgrade case) and L (empty states). **Pass B (Insights) deliberately NOT started** — range queries + streak logic + a ViewModel come before any UI, and it is its own session.
- 2026-07-29 — Phase 2 — **One global limit (D76) and strict mode (D77).** **(1) D76 — the per-platform daily limits collapse into ONE.** IG at 100 and YT at 100 meant a user could scroll 99 reels and 99 Shorts — 198 short videos — and be blocked by neither, because each app was only measured against itself. Worse and quieter: every platform arrived with its own fresh allowance, so ADDING one RAISED the ceiling — D73 turning YouTube on doubled everyone's real budget overnight and nothing said so. Now one slider, one key, one number. THE SPLIT THAT MATTERS: *what* counts is global (`BlockPolicy.blockingTotal` sums today across every platform cleared to block), *where* the block may draw stays per-platform (`blocksAtLimit` for the surface you are standing on) — so spending the budget on Instagram can never put a block over a SHADOW app. `blockingTotal` deliberately is NOT the bubble's grand total: TikTok/Snapchat run SHADOW on untoured markers and Snapchat is a known OVERcount (D32), so folding them in would block someone out of Instagram for scrolling their Snapchat inbox. MIGRATION: sum the limits the user actually chose — sum (not max) because it preserves the ceiling they already had and a migration must never tighten a limit on its own; actually-chosen (not defaults) because summing untouched defaults invents budget nobody asked for. Old keys kept, not deleted — a migration that destroys its input cannot be audited. **(2) D77 — STRICT MODE, the product call D75 deferred.** The free "5 more minutes" is deleted for good: constants, string, button, `onSnooze` handler and constructor parameter. Challenge or exit, no free bail. D50's asserted inequality goes with it (a comparison with one side left), replaced by a standalone boundedness assertion. CAUGHT IN PASSING: `accessibility_service_description` — the SYSTEM-facing string Play policy requires to match the service's actual function — still promised "you can always ask for a few more minutes", which the deletion made false; corrected. **INVARIANT 6 held and is now pinned by a test.** A snooze was never an exit, but the accepted consequence is real: on a device that can run no challenge at all the block panel is Exit ALONE, which is the state where a regression would be worst. New `BlockEscapeTest` asserts against the SHIPPING layout that every panel has its own Exit, no Exit carries a visibility/enabled attribute, Exit precedes the challenge button in traversal order, `block_challenge` defaults to gone (so the sensorless rendering IS the default), `block_snooze` is absent, the root is `BlockRootView` (focus-independent Back), and the stack sits in a fillViewport ScrollView. It reads XML because `BlockScreenController` needs a real Context and the unit-test android.jar is a throwing stub — same precedent as `GuiltPackTest` parsing the real asset. 300 unit tests green (+8 BlockEscapeTest, +3 BlockPolicy for the shared budget). PENDING on-device: HANDOFF Runs I (unified limit + migration + SHADOW exclusion) and J (strict mode + the sensorless Exit-only case). NEXT: the UI/UX appeal pass, planned this session, not yet built.
- 2026-07-29 — Phase 2 — **The block saga closes: the premature attach check was the whole bug (D72), and "5 more minutes" comes back (D75).** **(1) D72 — CONFIRMED ON DEVICE.** The instrumented build's transcript settles it: `attachedSync=false` → PENDING_ATTACH → `ATTACH LANDED (listener)` → SHOWN → `PROBE next-frame attached=true`. The window was healthy the whole time. `isAttachedToWindow` was being read one frame before `ViewRootImpl.performTraversals()` could set `mAttachInfo`, so the check had a 100% false-negative rate on a good window, and that one misread produced BOTH shapes of the bug — the un-dismissable full-screen trap before D71 (a healthy window abandoned untracked) and nothing drawing at all after D71 (the same window removed a frame early). **CORRECTIONS:** D52's "MediaTek ROM revokes SYSTEM_ALERT_WINDOW at runtime / `canDrawOverlays` lies while AppOps refuses" attribution is WITHDRAWN and D70's "the ROM is refusing anyway" framing is corrected — the ROM never refused anything. Both ADRs keep what they got right (D52's reframing and cooldown; D70's rule that a stale signal may darken a banner and never veto an attempt), and both now carry a navigation banner pointing at D72; reasoning left intact per the append-only rule. `OVERLAY_BLOCKED_BY_SYSTEM` and its copy are KEPT because they are now correctly scoped — only reachable after the 250ms deadline, which is a real refusal. PENDING_ATTACH + listener/next-frame/deadline stops being "a hypothesis under test" and becomes the permanent model. **(2) BUTTONS VERIFIED LIVE** in the same capture: `challenge.open` → done, `chooser.option[forehead_30]` → done, `challenge.exit` → done → launcher started. Block, chooser, challenge and exit work end to end; invariant 6 is demonstrated rather than argued. **(3) NO REBUILD WHILE SHOWN** — the two block cycles in the log (seq 83, 85) are the correct re-fire after exit while still over limit. Verified structurally, not just observationally: `showInternal` returns ALREADY_SHOWING on `view != null` and PENDING_ATTACH on `attachedRoot != null`, both before any inflation, and both fields are cleared only by `removeFromWindow` — so a second `block_root` requires an intervening teardown by construction. **(4) D73 KEPT** and now evidence-backed rather than premature: the same log counted five distinct YouTube channels with idle and likes correctly ignored, which is the identity-advance behaviour D34 predicted and D57 was waiting on. **(5) D75 — "5 more minutes" RESTORED**, reversing D74: constants, string, button, handler and constructor parameter, plus D50's asserted challenge-beats-free-tap inequality. The deletion was never requested and it narrows escape options on the block screen; it stays until there is a deliberate product call. Not a safety question either way — a snooze defers the block and keeps the user in the app, while Exit and Back (what invariant 6 names) were never involved. `BlockFailureInjector` stays, DEBUG-only, and is now more useful: it is the only way to exercise the genuine-refusal path the deadline guards. 290 unit tests green. NEXT: resume 3a — open the PR.
- 2026-07-28 — Phase 2 — **YouTube blocks; "5 more minutes" is gone (D73/D74).** Two owner decisions, each reversing an earlier one, plus a housekeeping fix. **(1) D73 — YT enforces its limit.** D57 had closed YT at BETA for v1 because the acceptance capture never materialised across four sessions; that evidence is *still* absent, and the decision is to ship the blocker anyway rather than leave a doomscrolling surface unenforced indefinitely. Implemented as the split D32 explicitly prescribed — a new `blocksWhileUncalibrated` flag ORed into `blocksAtLimit` — NOT as `maturity = STABLE`, because `Maturity` documents "calibrated against real swipes" (which would become false in the source) and `isBeta` drives the dashboard badge (flipping it silently withdraws the app's own disclosure that the YT number is unmeasured). Risk accepted: the block may fire a few Shorts early/late. Risk NOT taken: landing on the wrong screen — YT is `ENFORCED` on the toured `reel_recycler` (D26), so a marker miss undercounts. New test makes the override **illegal on a SHADOW platform**, so TikTok/Snapchat cannot inherit this as precedent. **(2) D74 — the free reprieve is deleted**: `block_snooze` button + string, `GRACE_MINUTES`/`GRACE_MS`, `OverlayController.onSnooze` and the `onSnooze` ctor param, all removed rather than hidden. Invariant 6 checked first and untouched — a snooze was never an exit. D50's `CHALLENGE_GRACE > GRACE` assertion had nothing left to compare against and was replaced with non-zero-and-bounded. **(3)** `OverlayDiagnostics.kt` was untracked and is now committed — a clean checkout could not have built this tree, which is exactly the class of defect 3a existed to end. **289 unit tests green, 5 new**, pinning the per-platform limiter (blocks AT its own limit, not one short; non-blocking platforms stay on the bubble at 10,000). **Device verification owed** for both decisions, and the D72 attach-timing question is still open — the Run A–D observations were not supplied, so no ADR was written for it.
- 2026-07-28 — Phase 2 — **The attach-timing diagnostic build. An experiment, not a fix — no ADR yet, deliberately.** D71's ownership fix turned the trap into a clean failure, and the clean failure exposed that the block never appears at all on the Nothing Phone: `addView returned but the view never attached ... canDrawOverlays=true`. **The hypothesis, and it retro-explains everything back to D52:** `View.isAttachedToWindow()` reads `mAttachInfo`, which is set in `ViewRootImpl.performTraversals()` — a Choreographer frame *after* `addView` returns, because `ViewRootImpl.setView()` only calls `requestLayout()`. So the synchronous check made since D52 reads a **healthy** window as failed. Pre-D71 the misjudged window was left in the WindowManager, attached a frame later, and became the un-dismissable full-screen trap the user actually saw — the orphan *was* the block. Post-D71 it is removed a frame early and nothing appears. D52's own capture ("rendered full-screen three times" *plus* repeated teardowns) is the same pattern, which means the AppOps/lying-ROM story may have been a misattribution from the start, and D70's stale flag was set by failures that were never real. **Built:** `ShowResult.PENDING_ATTACH` (the honest answer — the caller must not warn, flag or cool down on it); three observation points (`onViewAttachedToWindow`, which was wired and ignored this whole time; a next-frame `View.post`; a 250ms deadline backstop, `BlockLimits.ATTACH_DEADLINE_MS`); `OverlayDiagnostics` printing one greppable state block on a genuine refusal — exact exception class+message, `canDrawOverlays`, **AppOps `unsafeCheckOpNoThrow(OPSTR_SYSTEM_ALERT_WINDOW)` (diagnostic only, never a gate — D70)**, `bubbleAttached` (THE discriminating bit: same window type over the same app, so bubble-up + block-refused means the shape is the problem, not the device), device/model/SDK, and the exact params. **Also fixed, item 1:** `show()` is now total — the whole body is inside a failure boundary returning `FAILED`, not just `addView`; the crash at `OverlayController.kt:415` propagated out of an unguarded statement *before* the old try (`ChallengeAvailability`, `getString`, or a re-entrant detach callback) and, because `render` runs in a `Flow.onEach`, **the throw cancelled the collection and silently killed the count collector** — so the BLOCK branch is wrapped too. `attachedRoot = root` moved to *before* `addView` (a spurious ownership record is harmless; a missing one is the trap). The DEBUG injector's `no_attach` mode now lies at the probe, so it exercises the real failure path. **NOT built, deliberately:** the accessibility fallback (`performGlobalAction`) and the fast-retry ladder for the 30s cooldown that let 30→41 through. Both reverse D49 on Play-policy grounds, both are large, and if the hypothesis holds this device may not need either. Evidence first. **Green:** unit tests, `assembleDebug`, `compileReleaseKotlin`. **PENDING: HANDOFF Runs A–D**, plus the two answers that settle it — the `:415` stack trace and whether the block appears over a non-Instagram app.
- 2026-07-28 — Phase 2 — **Two block-screen defects, one of them a P0 against invariant 6 (D70/D71).** Backend/CI deliberately untouched; 3a's PR was paused for this. (1) **BUG 1 — the block refused to draw on a device where the overlay permission was granted.** The false signal was not `canDrawOverlays` and not AppOps (never called): it was `PermissionHealth.overlayRuntimeDenied`, the persisted `overlay_runtime_denied` pref. `canBlock` ANDed it in; `render` used `canBlock` as an early-return gate; the only code that cleared the flag lived in the success branch of `block.show()` — *behind that gate*. One refusal latched blocking off permanently, surviving re-grants and reboots, clearable only by wiping app data. The ordinary way to set it was `NO_PERMISSION`, i.e. **HANDOFF Run 3 itself** — the permission-integrity procedure bricked the feature it verified. This contradicted D52, which had already established that only the attempt knows. Fix: `canBlock` is now `accessibilityEnabled && canDrawOverlays` (queryable only), the observation moves to display-only `blockObservedBroken` (banner and `firstMissing` unchanged), the gate in `render` is deleted, and `show()` attempts `addView` first and consults the permission only afterward to *word* the failure. `ensureAttached` clears the flag on a successful bubble attach so a latched device self-heals on the next IG open. (2) **BUG 2 — the block trapped a user off their own home screen.** `show()`'s `!isAttachedToWindow` path returned `FAILED` while the root was still in the WindowManager and `view` was still null, so the window existed and nothing referenced it: `hide()`'s `val v = view ?: return` made Exit/snooze/challenge silent no-ops, `requestFocus()` was never reached so the root-focused Back listener never ran, `isBlocking` stayed false so it outlived leaving Instagram, and being OPAQUE + MATCH_PARENT + focusable it outranked the launcher. 30s later the cooldown lapsed and stacked a second one. Fix: `attachedRoot` records ownership the statement after `addView` returns; `removeFromWindow` is the only `removeView`; `hide()` is unconditional; `sweepOrphan()` runs on `offSurface` and destroy. Independently, Back moved from a root `OnKeyListener` (only fires while the root itself is focused — breakable by TalkBack, a dpad, or the trap path) to `BlockRootView.dispatchKeyEvent`, which is focus-independent. Panels wrapped in a `ScrollView fillViewport=true` so Exit cannot be laid out off-screen at large font scales. Every button, chooser row and Back now logs a **pair** of lines so "never registered" and "fired but did nothing" are distinguishable from outside the device. `BlockFailureInjector` (DEBUG-only, adb-armed like `DIAG_LABEL`, compiled out of release) makes the trap reproducible on demand — it ships because the fix's only other evidence would be the absence of an already-rare bug. `PermissionHealthTest` gains the D70 regression and inverts the assertion that pinned the old behaviour. **Unit tests green, `assembleDebug` + `compileReleaseKotlin` green.** PENDING and blocking: HANDOFF CURRENT Runs 1–4 on device — invariant 6 is not closed until Exit, Back and the forced-failure escape are all confirmed by hand.
- 2026-07-28 — Phase 3a — **CI exists, for both trees, for the first time (D60/D67/D68/D69).** Zero files under `app/` touched. (1) **THE FINDING, before a line of backend code: the repo did not build from a clean checkout.** 24 paths were untracked, including `Brand.kt`, `HoldDetector.kt`, `JumpDetector.kt`, `AccelPeakSource.kt`, both hold sources, `BlockRetryPolicy.kt`, four test files, the nav vectors, the `mascot_head` drawables and `PROJECT_MAP.md`. Every green build for weeks was local, against files git had never seen — which is the whole argument for 3a, delivered before 3a had written anything. Fixed by committing Phase 2 (D41–D58) first, then verified by cloning the branch to a scratch dir and building there with **no `local.properties`**: green in 20s, so CI needs only `ANDROID_HOME`. (2) **Two more landmines found the same way**: `gradlew` was mode `100644` in the index, so `./gradlew` on a Linux runner is `Permission denied` — fixed with `git update-index --chmod=+x` rather than a `chmod` step in the workflow, so it is fixed for every consumer and not just for CI; and there was no `.gitattributes` despite `core.autocrlf=true`, making line endings a per-machine setting, with `gradlew` now pinned `eol=lf` (a CRLF `gradlew` dies as `bad interpreter: /bin/sh^M`, an error that names the shell rather than the cause). (3) **BACKEND SKELETON, no domain endpoints.** `/health` static, `/readyz` an honest 503. Liveness and readiness are **separate modules** specifically so the no-DB rule is mechanically checkable: `test_health_has_no_db.py` walks the import closure from `app/routers/health.py` and fails on `sqlalchemy`/`asyncpg`/`app.models`/`app.db`/`redis`, and `test_health.py` proves the same at runtime against an unroutable DB, asserting the answer lands in under a second. Same posture as D45's source walk. (4) **GATES ALL BLOCK**, and all four live in `pyproject.toml` rather than the workflow so local and CI cannot disagree: ruff (incl. the flake8-bandit `S` rules — basic SAST from commit one), ruff format, `mypy --strict`, pytest at a **100% coverage floor** set to what the code actually achieves so it ratchets. 34 tests. The gates caught three things on their own first run, including `S105` on a fake token in the redaction test — exempted for `tests/` only, scoped to two rules, because in `app/` that rule will be right. (5) **SUPPLY CHAIN**: deps pinned by artifact hash and installed `--require-hashes`; base image pinned by **digest** because a tag is mutable; Trivy on image *and* filesystem with a **recorded posture** (fail on HIGH/CRITICAL that have a fix, `--ignore-unfixed` so the gate stays actionable); syft CycloneDX SBOM retained; every action pinned by full commit SHA with Dependabot as the other half of that decision. Image verified locally: builds, runs as **UID 10001**, `/health` 200, `/readyz` 503, 55.5 MB. (6) **BUDGET IS A CONSTRAINT** (D68): private repo, 2,000 min/month, so path filters + `cancel-in-progress` are the budget, not hygiene — with `security.yml` deliberately *un*filtered, because filtering a secret scanner by path is how you miss the file nobody thought of. (7) Python moved 3.12→**3.13** (D69) so the gates run identically local and CI; CLAUDE.md corrected rather than left stale. **NOT DONE: the first CI run has not been observed** — `android.yml`/`backend.yml` are PR-triggered and no PR is open yet, and there is no `gh` CLI on this machine. The D67 fallback ladder for the JBR-vs-Temurin daemon-JVM pin is written down and untested. NEXT: open the PR, work the first-run output, then 3b.
- 2026-07-28 — Phase 2 — **The brand pass (D58)** + YouTube closed as a v1 decision (D57). (1) YT — the Shorts capture was requested a fourth time and again not supplied, so promotion was REFUSED and the alternative branch taken: **v1 ships YouTube BETA, counting but never blocking**, with the promotion criteria written down. Instagram remains the only blocking platform. Pending is now a decision, not a question. (2) THE REAL FINDING — there was no purple to kill: `dynamicColor = true` meant the palette came from the user's WALLPAPER, so the app had no brand at all and the purple was just a default wallpaper's output. The parameter is DELETED, not defaulted false. (3) PALETTE SAMPLED FROM THE MASCOT rather than invented — cobalt `#0050B0` (cap) leads because this is a calming app that happens to be energetic; coral `#F08090` (brain) is `secondary` and never `primary`, since primary paints large fills and coral at that scale shouts. (4) ONE SOURCE ACROSS TWO RENDER PATHS was the actual engineering problem: the bubble/block/ring are plain views over other apps and can NEVER read `MaterialTheme`, which is why five files had drifting ARGB literals. New `Brand.kt` is plain Kotlin Longs that both paths consume, and **the XML layouts now carry zero brand colours** — `overlay_block.xml` lost its background and all five textColors, applied at inflation instead. One documented exception: `themes.xml`'s windowBackground, which paints before our code runs. (5) **The button hierarchy was an invariant-6 decision**: normal hierarchy would have made Exit the quiet ghost button, which is backwards — Exit now has the highest contrast on all three panels, snooze is the quietest. (6) State colours kept green→amber→red semantics (severity before style) but retuned to mint + the mascot's own brim orange and sneaker red; **thresholds untouched and `BrainStateTest` passes unmodified**, which is the proof nothing logical moved. (7) Full type scale, negative tracking on the display sizes; still Roboto — licensing a display face is a product call, and the swap is one `FontFamily` edit. (8) The nav bar lost its 🧠: importer gained a `head` family whose crop box was MEASURED at true 24dp (aspect 1.03), plus two hand-written vectors so all three tabs changed together. Build green: 284 tests, `assembleDebug` + `compileReleaseKotlin`; audited that no brand ARGB literal survives outside Brand.kt. NEXT: on-device light/dark sweep (HANDOFF), then Phase 3.
- 2026-07-25 — Phase 2 — Challenge 4 of 4: forehead hold (D55) + doc/vault optimization (D56). **The challenge suite is complete.** (1) FOREHEAD — `ProximityHoldSource` pairs proximity NEAR with device UPRIGHT, because covered alone is a thumb on a table. NEAR compares against `sensor.maximumRange` rather than a hard-coded 5cm, since many devices report only binary 0/max. Two sensors on ONE listener so teardown is a single `unregisterListener` that cannot half-release, with cleanup on partial registration. `HoldDetector` reused **verbatim** — the whole increment was one new file plus four small edits, which is what keeping it pure bought. (2) THREE TESTS CAUGHT THEIR OWN STALENESS this increment, all the same failure mode — a test pinning "what is not built yet" by name: `forId("forehead_30")` was the unknown-id sentinel and started existing, and `IMPLEMENTED` was pinned to a hand-listed subset. Both rewritten to assert the *property* (lookup rejects nonsense; IMPLEMENTED ⊆ declared) with a comment naming what to change when the next strategy is declared ahead of its code, so they stop breaking every increment for no reason. (3) DOCS (D56) — `PROJECT_MAP.md` is now an explicit single entry point with a read-order rule (`CLAUDE.md` → map → the one doc the task needs) and a table saying what each doc holds *and when to open it*, including the `ScrollKiller/` Obsidian vault the map had never mentioned. `DECISIONS.md` gained a **56-line title index** so ADRs are scannable without their bodies — additive, all 54 original entries byte-intact; a mechanical retro-split was rejected on evidence (only 34/54 parseable, ~20 needing hand judgment in an append-only log). D55+ leads with its title on its own line, greppable via `^\*\*D`. Link policy split deliberately: markdown links in `docs/` (graph-visible AND GitHub-clickable), wikilinks in the vault. Stale vault notes refreshed — `Challenges.md` still listed two challenges with the holds "declared only". Fence-balance guard added to HANDOFF; all files verified even. Build green: 284 tests pass, `assembleDebug` + `compileReleaseKotlin` succeed. NEXT: the brand pass. Still open and unrelated: YouTube `Maturity.BETA`, fake-scroll feed, guilt pack T3/T4 expansion.
- 2026-07-25 — Phase 2 — Challenge 3 of 4: face-down hold (D54) + project map refresh. Increment B1. (1) HOLDS ARE TIME, and that broke the engine's monotonicity assumption for the first time. `HoldDetector` (pure, 9 tests) times a condition and **forgets the hold on a break** — the flip-flop test asserts five five-second holds total zero, which is the anti-cheat. `ChallengeProgress.onHoldElapsed(seconds)` is absolute+clamped like `onCumulative`, so the reset needs no separate signal; the old "completion is sticky" doc comment was CORRECTED rather than preserved, with a test pinning the non-stickiness. (2) TWO PROBLEMS THAT WERE NOT IN THE BRIEF and would have shipped broken: the default 30s **screen timeout** is exactly the hold length, so the display slept mid-hold and the accelerometer stopped → `FLAG_KEEP_SCREEN_ON` scoped to the challenge panel, cleared on every teardown path, requiring `BlockScreenController` to retain its LayoutParams; and the ring is **pointed at a table** during a face-down hold, so haptics (400ms complete / two-pulse break, `VIBRATE` normal permission) are the only feedback — without the break buzz the user flips up to check and destroys the hold they were checking. (3) `ProgressUnit {COUNT, SECONDS}` with a pure label function; ring counts down, arc still fills; `ProgressRingView` stays dumb and is handed the string. (4) A registry test **caught its own staleness**: `surpriseMe never returns the avoided challenge` had pinned the survivor by NAME, which only held while the pool had two entries — rewritten to assert the exclusion across every candidate. Build green: 284 tests pass (was 266), `assembleDebug` + `compileReleaseKotlin` succeed. (5) `docs/PROJECT_MAP.md` refreshed to D53/D54, stray unclosed code fence fixed, and **YouTube's `Maturity.BETA` promoted from a soft "pending" note to an explicit ⚠️** — it has been pending across several sessions and the map was understating it. NOT DONE: forehead (B2); `IMPLEMENTED` still excludes `PROXIMITY_HOLD`. Next: on-device HANDOFF runs for face-down, then B2, then the brand pass.
- 2026-07-25 — Phase 2 — Challenge 2 of 4: jump + the chooser (D53). Increment A of the three-remaining-challenges plan. (1) JUMP — `JumpDetector` is pure Kotlin and counts a free-fall→landing SEQUENCE, because a landing spike alone is clearable by shaking the phone; 11 tests cover shake-rejection, landing ringing, a bounce that re-arms inside the refractory window, stale arms, the dead band, and both baseline paths (implausible → 9.81, badly-calibrated-but-plausible → kept). `AccelPeakSource` is the thin half: magnitude, `SENSOR_DELAY_GAME`, no batching, `elapsedRealtime` rather than the vendor-unreliable `SensorEvent` clock. (2) CHOOSER — third panel in the same window, rows built from availability, "Surprise me" excluding the previous draw, own Exit listed first. `showPanel` centralises the exactly-one-visible invariant. (3) ENGINE — `ChallengeController` no longer owns a fixed `StepSensorSource`; sources resolve per attempt via a `ChallengeSensorSource` interface + `ChallengeSensors.sourceFor` factory that returns null for unbuilt strategies. `ChallengeAvailability` replaces the app-wide `MotionStatus.isAvailable` question on the block screen (per-spec now — the accelerometer needs no permission); `MotionStatus` itself untouched, Settings and PermissionHealthReader still depend on it. (4) Reward stayed flat at 15 min for all challenges; the domination argument from D50 was weighed and rejected because per-spec pricing would re-punish whoever picked the easier option *because* of a constraint. Build green: 266 tests pass (was 236), `assembleDebug` + `compileReleaseKotlin` succeed. Lint's only error is pre-existing and in gitignored `local.properties`. NOT DONE: face-down and forehead (increments B and C); `IMPLEMENTED` still excludes `ORIENTATION_HOLD`. Next: on-device HANDOFF runs A–E for jump + chooser, then increment B.
- 2026-07-27 — Phase 2 — Count tokens, 7-day no-repeat, and the pool-size reckoning (D47). NO detection change, NO cadence change, NO intensity-threshold change: GuiltCadence.SCHEDULE and GuiltThresholds are byte-for-byte what D46/D41 shipped. **YouTube's "first Short after entering isn't counted until a scroll" is KNOWN and was explicitly ACCEPTED going in** — deferred deep-link entry credit, batched separately, untouched here. YT is still `Maturity.BETA`. (1) COUNT-IN-TEXT BUG FIXED. Lines baked the number in, so once D46 started re-showing them all day the app announced "One-fifty" at 312 scrolls beside its own counter reading 312. Lines now reference the count only through a `{count}` token, plus `{minutes}` — a duration claim ("that is half an hour") is the same bug in different units, derived from the count via TimeEstimate, and without the second token the eight strongest existential lines would have had to be deleted rather than fixed. Substitution happens on EVERY render, not once at fire time, so a line pinned at 310 and still on screen at 314 reads 314; freezing it would reintroduce the contradiction at one remove. **THE GUARD IS THE PART WORTH READING.** The brief asked for a bare-integer check. A bare-integer check would have caught ZERO offenders — every single one was spelled out (Fifty, Seventy, A hundred, One-fifty, Seventy-five minutes). So `GuiltText.offendingNumber` bans digits AND scale words (twenty…ninety, hundred, thousand, century, dozen) AND time units not preceded by `{minutes}`, while deliberately allowing one/two/three, which in this pack are always idiomatic ("one more", "one full lecture") and would make the guard something people switch off. It runs in TWO places for two failure modes: the parser DROPS an offending line (a bad remote pack degrades per D33 rather than shipping the bug) and a test FAILS THE BUILD on the bundled pack (our own content silently shrinking is not acceptable). Running it found three offenders my manual audit had missed — "Century scored", "another hour between friends", and one genuine false positive ("any minute now") that was rewritten rather than exempted, because a blunt guard everyone trusts beats a clever one with an exception list. 27 of 72 lines rewritten; 26 now carry a token. (2) SEVEN-DAY NO-REPEAT. The old rule was session-scoped and pool-cycle-based, and at D46's cadence a pool cycles in minutes, so "no repeat" had quietly stopped meaning anything. Now a rolling 7-day window over a persisted `guilt_shown` table (lineId PK + wall-clock millis; DB v2→v3, migration SQL verified byte-for-byte against Room's exported 3.json). Last-shown only, not a log: the only questions asked are "inside the window?" and "which is stalest?", and a full log would grow without bound at the top of the cadence for no extra information. The selector answers synchronously on an AccessibilityService render path, so the table is read ONCE at process start into an in-memory `GuiltHistory` and written through after; the load window is a deliberate, bounded hole (the alternative is blocking a render on Room) and `seed()` merges on max so a line shown during it cannot have its timestamp dragged backwards by disk. Wall clock for history, monotonic for D46's gap — persisted values must survive reboots, durations must survive clock changes — and both now travel in one `GuiltNow` so that distinction is visible at every call site. (3) RECONCILED WITH D42. The daily deck NARROWED each tier to ~60%/day to manufacture freshness; the 7-day exclusion does that far more strongly, and both narrowing would starve the pool — 60% of an already week-thinned pool is exactly how a healthy pack reaches the fallback. So the narrowing is DELETED (forDay/baseDraw/deckSize gone, not left vestigial) and the seed survives as an ORDERING: exclusion is the hard rule, the daily seed is the tiebreak. It is inert in the common path (the weighted draw ignores order) and earns its keep in the fallback, where lines tie at "shown today" and the tiebreak must be deterministic and per-install. (4) GRACEFUL, LOUD EXHAUSTION. Falls back to the least-recently-shown QUARTER of the pool — a slice, not the sorted pool, because the weighted draw ignores order and would happily re-pick the line from a minute ago; never-shown lines sort first. Fires an `onPoolExhausted` callback (a callback, not a Log call, so the selector stays Android-free and a test can assert it) that GuiltLines turns into a warning quoting GuiltPoolMath: "tier EXTREME has 18 lines and every one was shown in the last 7d. At 400 scrolls/day this tier needs ~434. EXPAND THIS TIER." (5) **THE POOL-SIZE FINDING — the deliverable, and it is uncomfortable.** GuiltPoolMath derives consumption by running the REAL GuiltFiring over a simulated day, so it cannot drift from what ships and re-derives itself if the cadence is retuned. Fires/day by tier — T1: 1, T2: 1, T3: 5, T4: 1 @150 / 18 @300 / 62 @500 / 196 @800 scrolls. Tiers 1–2 fire exactly once a day BY CONSTRUCTION (below the schedule's first row, so only their crossing fires), so 7 lines each sustains anyone forever and the shipped 18 is ample. Tier 3 spans exactly 100–149 at every 10, so it is permanently capped at 5/day: 35 sustains a week for ANY user and 18 does not — a real but finite, cheap-to-close shortfall. Tier 4 is unbounded and is the whole problem: **126 lines for a 300-scroll/day user, 434 at 500, 1,372 at 800, against a shipped 18.** Recommendation is deliberately NOT "write 1,372": target 40 for tier 3 (closes it permanently) and 150–200 for tier 4 (covers the realistic heavy user at ~300/day, an 8–11× expansion), then let the fallback carry the extreme tail while the log names who is hitting it. The alternative not taken, because D46 was to stay intact: flatten the cadence above ~500, where the schedule consumes content faster than anyone can author it. Numbers pinned in GuiltPoolMathTest — including one test asserting the CURRENT inadequate coverage, so it passes today and fails the moment the pack grows. The build is deliberately NOT failed on the shortfall; blocking every unrelated change until a thousand lines exist is the wrong lever. (6) Settings → Clear data now also wipes the guilt history — it is the user's data about them, and it keeps the on-device runs repeatable. **192 unit tests pass** (+26: GuiltTextTest 13, GuiltHistoryTest 7, GuiltPoolMathTest 6, plus GuiltSelectorTest 13→15 and GuiltDeckTest rewritten 10→7 for the narrowing removal), `assembleDebug` + `compileReleaseKotlin` GREEN, `lintDebug` unchanged (its one error is still the pre-existing `local.properties` backslash escaping). PENDING on-device: HANDOFF Runs 1–5.
- 2026-07-27 — Phase 2 — Guilt lines repeat on a count-escalating cadence (D46). NO detection change and NO intensity-threshold change: GuiltThresholds' 50/70/100/150 are byte-for-byte what D41 shipped, D11's 200ms IG quiet gap and YT's 500ms identity floor are untouched, and YouTube is still `Maturity.BETA`. **The YT "first Short after entering isn't counted until a scroll" behaviour is KNOWN and was explicitly ACCEPTED this session** — it is the deferred deep-link entry credit, batched separately, and nothing here touches it. (1) THE MODEL CHANGED, not the content: D41 fired one line per tier crossing and then went silent for the rest of the day, so at 400 reels the app had already said everything it was going to say — exactly backwards. Firing is now a repeating cadence driven by the daily total, on a NEW axis deliberately separate from intensity. `GuiltTier` still decides how HARD (which pool); `GuiltCadence` decides how OFTEN. Neither reads the other's numbers, so the two are tunable apart, and they compound at the top by design: at 800 reels the lines are both the most savage the pack has and as frequent as the display allows. (2) THE SCHEDULE is one ordered table and nothing else reads a threshold or an interval directly — from 100 every 10 scrolls, 250 every 7, 320 every 5, 400 every 4, 500 every 3, 600 every 2, 800+ every scroll. Declared ascending (how it is reviewed), descending scan derived. Below 100 `intervalFor` returns NULL rather than a sentinel, so 50 and 70 keep D41's behaviour exactly — they fire on the tier crossing and then go quiet, because a repeating nag at 55 reels is the opposite of the design. Crossings still fire at 100 and 150, so an escalation announces itself instead of waiting out an interval it landed mid-way through. (3) WHEN is `GuiltFiring`, pure and separately unit-tested, because the schedule is a lookup table but this is where every subtle edge lives. Three are load-bearing: BASELINING (first observation of a day or process records the count and fires nothing — otherwise walking into Reels at 300 reads as `300 - 0 >= 7` and fires instantly, and again on every tab back in; the baseline PERSISTS across surface exits, so `offSurface()` no longer resets anything and re-entering at an unchanged count is silent — the cadence measures SCROLLS, not arrivals, and time alone never fires); BACKWARDS COUNTS re-baseline, since within a day that means a data clear and the next scroll would otherwise read as hundreds at once at a tier the user has left — same path handles the day rollover; and SUPPRESSION IS PENDING, not dropped — a gap-blocked fire consumes no state, so it goes out on the first emission after the gap, which also stops a gapped TIER CROSSING from being swallowed. Dropping it would mean the faster you scroll the fewer lines you see. (4) MIN-DISPLAY-GAP = 5.5s, deliberately much larger than the ~1.5s the brief suggested, and this is the one call worth arguing with. The schedule governs ELIGIBILITY in scrolls; the gap governs DISPLAY in seconds, and at the top those genuinely disagree — at 800+ every scroll is eligible and a fast scroller produces one a second. A gap shorter than the 4s a line is displayed for means every line is overwritten before it can be read, so at the exact moment the app is trying hardest to be heard nothing on screen is legible; worse, the pill never collapses and the bubble stops being a counter at all. The counter is the product; the lines are commentary on it, and commentary must not evict what it comments on. So 5.5s = the full 4s display + a guaranteed 1.5s counter-only window, DERIVED from its two parts rather than written as a literal, which is also why `DISPLAY_MS` moved out of OverlayController into `GuiltCadence` beside it — split across two files those numbers drift and the guarantee quietly stops holding. Practical ceiling at 800+ is ~11 lines/min. Clock is `SystemClock.elapsedRealtime`, not wall time: the gap is a DURATION and a tester changing the device date (which the handoff asks for) must not be able to jam it open or shut; the day key stays wall-clock-derived because it is a date. (5) FIRING IS THE OVERLAY'S ALONE (`GuiltLines.fire`), even though Home also observes the count — a fire is something you SHOW, and two callers would race for it with the bubble silently losing a nudge whenever the ViewModel won, which is reachable because `WhileSubscribed(5s)` keeps Home collecting for five seconds after the user switches to Instagram. Home reads `current()`, which returns whatever `fire()` last pinned, so "Home shows the latest fired line" falls out of the existing pin with no second mechanism; `current()` keeps its lazy draw for when the overlay never runs at all (bubble off, or overlay permission never granted). ACCEPTED consequence: a gap-suppressed tier crossing lets `current()` repin a correct-tier line for the panel immediately and `fire()` draw a second when the gap elapses — both current-tier, the one-line-at-a-moment invariant holds throughout, and the panel updating instantly on a crossing is arguably better anyway. (6) CONTENT SELECTION UNTOUCHED: `fire()` calls the same draw path, so a rapid-fire line is still tier-correct, still from today's deck, and still cannot repeat back-to-back — the no-repeat rotation is what keeps "every scroll" from degenerating into the same three lines. NO WINDOW CHURN: `showNudge` is text on the already-attached view plus one re-placement, exactly like a digit-count change (D30/D38 hold), which matters far more now that it runs dozens of times a day rather than four. **166 unit tests pass** (+19: `GuiltFiringTest` 14, `GuiltCadenceTest` 4, plus a fixture fix), `assembleDebug` + `compileReleaseKotlin` GREEN, `lintDebug` unchanged (its one error is still the pre-existing `local.properties` backslash escaping). PENDING on-device: HANDOFF Runs 1–4 below — every 10 at 100/110/120, every 5 at 320+, every scroll at 800+, and that the gap keeps the lines readable and the counter visible.
- 2026-07-27 — Phase 2 — Escalating, daily-refreshing, data-driven guilt lines (D41–D45). NO detection change: no threshold, debounce, marker, gating, limit or calibration touched. D11's 200ms IG quiet gap and YT's 500ms identity floor are byte-for-byte what they were, and YouTube is still `Maturity.BETA` — the deferred YT→STABLE promotion and the deep-link entry credit are still NOT done and are batched for next session. (1) ESCALATION (D41): the pack had one flat pool, so every line was equally likely at reel 5 and reel 500 — the app opened at full volume with nowhere to go. New `GuiltTier {MILD/MEDIUM/STRONG/EXTREME}` maps the day's TOTAL (not per-app) to intensity 1–4 at 50/70/100/150, with all four thresholds named in a `GuiltThresholds` object that contains nothing else. Every line declares an `intensity`; a tier draws only from its own band, so escalation is CONTENT rather than volume or styling. **Below 50 the app says nothing at all** — `forCount` returns null and every surface renders no line; that silence is load-bearing, because an app that comments on 40 reels has cried wolf by the time the number matters. Empty band widens DOWNWARD only (a mild line at 160 is underwhelming; a savage line at 52 is the failure the tiers exist to prevent), and a test over the bundled asset proves the upward last resort never fires. Deliberately NOT BrainState's 50/150 — that decides the mascot pose, this decides what the app says; they agree today and must stay tunable apart. Schema bumped 1→2. (2) DAILY FRESHNESS (D42): `GuiltDeck` narrows each tier's pool to 60% per day (floor 6) via a seeded shuffle over FNV-1a(dayKey|installId) — pure, nothing persisted, so two surfaces on the same day get the same deck and a mid-day restart doesn't shift it. Install id is a locally-generated UUID (a rotation seed, NOT a device identifier) so users don't rotate in lockstep. Lines that sat out yesterday are prioritised back in; the reference set is yesterday's BASE DRAW rather than its final deck, because the latter is circular and no finite cut is exact — the weaker guarantee is the one written down and tested, plus a second test asserting the deck still churns against yesterday's REAL deck. Pool canonicalised before shuffling, so reordering the JSON can't reshuffle everyone. (3) ONE PICKER (D44): `GuiltSelector.current()` PINS one draw per (tier, day, pack, locale), so Home's header, the bubble's threshold nudge and the expanded panel are structurally incapable of disagreeing; `invalidate()` on app open (onCreate, not onResume) is the one thing the key can't express. The bubble nudge moved from BrainState flips (50/150) to tier crossings (50/70/100/150), with a separate `hasTierBaseline` flag because "not rendered yet" and "under 50" are both a null tier — conflating them would swallow the most important nudge there is. Block screen keeps its own unpinned draw (own moment) but shares decks/rotations/no-repeat and is tier-filtered; its tone policy now excludes reverse_psych, since "go on, one more" on a locked screen reads as broken. (4) AUDIENCE (D43): lines carry lang/region (defaulted from the pack's own top-level values), `GuiltPack.pool` filters audience → tone → intensity with an explicit fallback at each step, and Settings ships a pack picker with one option ("India — Gen Z"). Adding a pack is a JSON drop + one catalogue entry + one string — no engine change. `GuiltPackLoader.install()`'s hot-swap and revision guard are untouched; selection moved OUT of the loader into `GuiltSelector`, which detects a swap by comparing `GuiltPack.identity`. (5) CONTENT: 72 lines, ~18 per tier, India Gen-Z English, all four categories at every intensity including PRIDE at tier 4 (D9 — at 200 reels the way out still has to exist). (6) NO HARDCODED LINES (D45): enforced by the build, not by review — a test walks `app/src/main/java` and fails if `GuiltLine(` appears outside GuiltPack.kt and GuiltPackParser.kt, and pins the crash fallback at exactly 3 lines. (7) PANEL NAMES (D44c): D40's generic glyph said "some video app", not Instagram, so the bar rows carry a new `PlatformSpec.brandName` as TEXT beside the glyph — a word, never a logo (naming an app is nominative use; reproducing its mark inside its own UI is not). Panel min width 180→240dp so the bars stay long enough to compare. `iconRes` slot unchanged for licensed marks. **147 unit tests pass** (+39: GuiltTierTest 5, GuiltDeckTest 10, GuiltSelectorTest 13, GuiltPackTest rewritten to 19 incl. the content invariants), `assembleDebug` + `compileReleaseKotlin` GREEN, `lintDebug` finds nothing new (its one error is still the pre-existing `local.properties` backslash escaping). PENDING on-device: the five HANDOFF runs — line appears at 50, escalates at 70/100/150, differs across two days, agrees across Home + bubble at one moment, and no hardcoded string anywhere.
- 2026-07-25 — Phase 2 — Panel containment, count latency, platform icons (D38/D39/D40). NO detection logic, threshold, limit, marker or gating change; D11's 200ms IG quiet gap and YT's 500ms identity floor are byte-for-byte untouched. (1) PANEL OFF-SCREEN FIXED (D38). Root cause was not the panel's layout but the WINDOW's: it is WRAP_CONTENT with `FLAG_LAYOUT_NO_LIMITS`, which is deliberate and load-bearing (it is what lets the compact pill be dragged flush to a real screen edge) and which also means WindowManager will never pull the window back as it grows — and expanding makes it ~5x taller. So the window now contains itself. Pure, unit-tested `OverlayPlacement` decides x/y per shape: COMPACT clamps the drag position into the usable rect, EXPANDED centres horizontally and keeps the pill's y clamped so the bottom fits. Usable rect from new `OverlayMetrics` — API 30+ `currentWindowMetrics` + `getInsetsIgnoringVisibility(systemBars|displayCutout)` (insets-ignoring-VISIBILITY on purpose: the overlay sits on a full-screen player that hides the bars, so live insets there are zero and a panel placed against them would be under the status bar the second the bars animate back), API 26–29 real display size minus the framework's `status_bar_height`/`navigation_bar_height`. CENTRED rather than anchored to the bubble because an anchored panel near an edge is either half off-screen or shoved inward by a distance that varies with its width, which reads as the panel moving at random. Gravity stays TOP|START in BOTH shapes so `params.x/y` always mean the same thing (CENTER_HORIZONTAL would make x mean "offset from centre" expanded and "offset from the left" compact — two meanings for one field the drag handler also writes). Compact drag position tracked separately from `params.x/y` so collapsing returns the pill where the user left it, and the CLAMPED position is what gets stored so overshoot leaves no dead zone. Drag collapses an open panel first. Rotation/fold/multi-window via `BubbleView.onConfigurationChanged`, posted so the view has re-laid-out before we measure. View is measured against the rect BEFORE placement, so the panel lands right on its first visible frame instead of jumping. D30 HELD: every path is `updateViewLayout` on the already-attached window, skipped entirely when x/y already match. (2) COUNT LATENCY (D39) — fix shipped, MEASUREMENT NOT DONE. Diagnosis is structural, not measured: the debounce is NOT a latency source on IG (SwipeDetector resets its quiet gap on every DOWN, so the first DOWN of a swipe counts immediately) and the bubble refreshes on every distinct emission, leaving the read path as the only unbounded layer — the number the user saw was gated on TWO separate SQLite commits (the aggregate upsert and the raw-event insert are separate suspend DAO calls, so separate transactions) plus Room's InvalidationTracker round trip. So `record()` now bumps an in-memory `PendingCounts` synchronously on the caller's thread before dispatching the write, and reads merge Room with it by `max` per platform (pure `TodayMerge`) — max works because pending holds a running TOTAL, not a delta, so the two converge as writes land and the count is monotonic within a day. Room remains the source of truth and the only durable store; nothing increments pending that does not also go to Room in the same call. `clearAll()` resets pending AFTER both deletes (resetting first would strand an advance detected mid-clear as a permanent +1, since pending is a running total); a day rollover drops it by date; the day key is now resolved on the caller's thread so both halves file under the day the advance was DETECTED on. All read Flows became projections of `observeTodaySummary()` (`observeBreakdown()` deleted, DashboardViewModel shares one `stateIn`) so no surface can miss the optimistic path — D35 restated. DEBUG-only `CountLatency` prints one `LATENCY` line per advance attributing DETECT→OPTIMISTIC→PERSISTED→EMITTED→RENDERED. **The deltas were never captured**: the debug APK is installed on the attached device and the service is confirmed alive and logging, but the phone is PIN-locked so the Reels/Shorts surface was unreachable. HANDOFF Run 2 is the measurement and it is the gate on believing this fix. Left deliberately untouched pending it: YT's `IDENTITY_SCAN_MIN_MS = 250ms` content-change rate limit, which can sit on a real identity change for up to 250ms — a rate limit, not D11 calibration, and the prime suspect if Run 2 shows `detect→emitted` already small while YT still feels late. (3) PLATFORM ICONS (D40) — `IG`/`YT` text in the panel's bars became monochrome glyphs via a new `PlatformSpec.iconRes`, tinted to the same recessive white, in a column the same width so the bars still line up. Glyphs are deliberately GENERIC (Material Icons geometry, Apache 2.0 — camera / video player / music note / flash bolt), NOT brand marks: the panel draws inside those apps' own UI and the bar already carries the count, so reproducing a logo is a trademark question with no upside. Licensed marks are a drop-in at the same filenames with no code change. Row lookup uses the new `PlatformRegistry.specOrNull` (`specFor` throws for a Platform with no enabled spec — Facebook). 108 unit tests pass (22 new: `OverlayPlacementTest` 12, `TodayMergeTest` 10), `assembleDebug` green, `lintDebug` clean apart from the pre-existing `local.properties` escaping error. PENDING on-device: all four HANDOFF runs, Run 2 above all.
- 2026-07-25 — Phase 2 — Bubble mascot fill + tap-expand stats panel (D37). No detection, threshold, limit or gating changes. (1) DIAGNOSED the "mascot doesn't fill the bubble" report and it was not the suspected transparent padding: three of four masters were still RGB with NO alpha (D36 shipped them opaque), so `union_trim()`'s alpha guard fired on every run and the trim had NEVER executed — the 40dp bitmap was the full square canvas with the character at ~70% x 75% of it, opaque for cracking/fried (a dark box that blended into the near-black pill, which is why it read as "small" rather than "there's a square on my screen"). (2) IMPORTER now keys the backgrounds itself, reversing D36's no-auto-keying on a real distinction: D36 refused a COLOUR key (correctly — the backgrounds are gradients and any colour key eats the character's outline), while this is a border-seeded GEODESIC FLOOD keying on SMOOTHNESS (reachable from the border without crossing a neighbour delta > 8). Tolerance 8 came off a measured stability plateau, not taste — kept area is flat at ~35% for tol 4..8 and jumps as the flood drains the character (fried leaks at 10, guardian 12, cracking 24); `assert_no_leak()` now fails the import if a future master leaks. (3) The union trim runs (crop 100% → 71.7% x 87.4% of canvas) and output is emitted at the crop's NATURAL ASPECT with the longer side at base_dp (33x40dp bubble, 98x120dp hero) — the old `resize((px,px))` was only safe while the crop was the full square and would have stretched the character the moment the trim started working. `HERO_DP`/`BUBBLE_DP` now mean HEIGHT; Home's hero and the block screen bind height and let width follow. MEASURED at true 40dp/xxhdpi: character fill 73.3% → 84.2% of the mascot box in height, 68.3% → 94.9% in width, opaque area 35.6% → 56.6%. Residual ~14% vertical slack is the deliberate price of the SHARED crop (per-state crops would make the mascot grow when you cross 50). (4) BUBBLE TAP now expands a breakdown panel IN PLACE instead of launching Home — total + estimated time, then one proportional bar per platform with a count above zero, longest first; tap again collapses. Compact pill is now ONLY mascot + grand total (D35's `IG 30 · YT 10` subtitle is superseded by the bars). Mascot became a height-bounded ImageView; a compound drawable draws at intrinsic size in a box the TEXT defines, so it could never have been sized to the pill. (5) D30 HELD BY DESIGN, not by luck: the invariant is one window attached once, and it is the ROOT going GONE that frees a surface — the root stays VISIBLE and only a CHILD toggles, so expanding is a WRAP_CONTENT re-measure (a resize of the existing surface) exactly like digit-count changes already were. Bar rows built once and shown/hidden, panel filled while collapsed, bar length via layout weights (a pixel width would need the unmeasured track). (6) New pure `BubbleBreakdown` derives bar shares from the SAME `TodaySummary` emission as the headline total, so bars and total cannot disagree; drops count<=0 rows and keeps declaration order on ties. View tree extracted into a new `BubbleView` so the controller didn't become a God-class. 86 unit tests pass (9 new), `assembleDebug` green. PENDING on-device: fill ratio eyeball, tap-expand/collapse, and a BLASTBufferQueue churn check while toggling the panel (HANDOFF).
- 2026-07-25 — Phase 2 — Mascot art integration, art-only (D36). No detection logic touched. (1) The emoji brain is GONE from all three render surfaces: Home's hero is an `Image`, the bubble is a start-side compound drawable on the existing TextView (one view → one window → one surface, per D30), and the block screen is an `ImageView` showing the new GUARDIAN pose. Only remaining glyph is the bottom-nav tab 🧠, left deliberately (20sp chrome). (2) `BrainState` lost its `emoji` field and stays Android-import-free; art resolves through a new `MascotArt` object — the single place a future art swap touches. Thresholds (50/150) untouched, all 77 tests still pass unmodified. (3) Pipeline: masters in `art/mascot/<state>.png`, `tools/mascot_import.py` is the only path into res/ and is idempotent → swap = overwrite master + re-run. 35 pre-scaled PNGs (2.6MB) across mdpi…xxxhdpi in two families, 120dp hero and 40dp bubble, verified present in the APK. (4) BLOCKED ON ART: cracking/fried/guardian masters are RGB with a dark vignette background and will render as opaque boxes until background-removed and re-imported; only `healthy` is final. No auto-keying — the backgrounds are gradients and any key eats the character's outline. Shared-crop trim is written but auto-skips until every master has alpha. (5) NOTE — YouTube is still `Maturity.BETA` in `PlatformSpec`; the promotion to STABLE and prior-session fixes 1–4 (deep-link entry credit, bubble info panel, idle line, open line) are NOT in the tree. Next: background-removed masters → re-run the importer → on-device HANDOFF runs A–C, then the full visual/brand pass.
- 2026-07-25 — Phase 2 — YT advance signal SOLVED + bubble shows one total (D34/D35). (1) YT — the D31 capture finally landed and was conclusive (outcome C): `TYPE_VIEW_SCROLLED` is dead on Shorts (deltaY=0 on every event), and the per-Short signal is an identity — the channel handle — on `TYPE_WINDOW_CONTENT_CHANGED`, which fires 40+ times per Short. So counting keys off the identity CHANGING, never the event. Added `AdvanceStrategy {DELTA_Y_FORWARD, IDENTITY_CHANGE, EVENT_PULSE}` to PlatformSpec (replacing the placeholder `DetectionStrategy`); IG stays DELTA_Y_FORWARD and is untouched, YT is IDENTITY_CHANGE. New `ReelIdentity` (anchors UP to `reel_player_page_container`, so all 40 events of one Short resolve to the same node; takes the leading `@handle` token, else an allowlisted title node, else NOTHING — "Auto-dubbed" / "…79K other people" / subtitles are all rejected by construction) and new pure `IdentityAdvanceDetector` (counts on change, 500ms floor, returns COUNTED/UNCHANGED/UNREADABLE/FLOORED so the transcript names the rejection). `onContentChanged` is the new counting path, rate-limited to 250ms BEFORE any node work so it's affordable in release; D29's entry credit is suppressed for identity platforms (it would double the landing Short). Service KDoc's privacy claim corrected — shipping detection now reads the minimum node text, compares, and discards; nothing is stored, synced, or logged outside DEBUG. YtProbe rewritten from discovery to ACCEPTANCE (new identity branches, decision table describes the shipped logic + the two runs). (2) BUBBLE — was binding to `observeToday(platform)`, so 30 reels + 10 shorts read "30" or "10", never 40. Renamed `observeToday()` → `observeTodayTotal()`, added `TodaySummary {total, perPlatform}` + `observeTodaySummary()` derived from the existing breakdown query (one collector, can't disagree); bubble now renders `🧠 40` + a dimmed `IG 30 · YT 10` (top 3 non-zero, new `PlatformSpec.shortName`) via SPANS in the existing TextView — no second view, no second window, D30's no-churn property intact. Brain state now follows the total, so bubble and Home finally agree. Block still asks the per-platform question. testDebugUnitTest GREEN (77 tests, +20: IdentityAdvanceDetectorTest, ReelIdentityTest fed the literal capture strings, plus registry invariants "IDENTITY_CHANGE implies non-empty identityAnchors" and distinct shortNames); assembleDebug + compileReleaseKotlin GREEN. YT REMAINS BETA. PENDING on-device (gates promotion to STABLE): YTPROBE_SWIPE 15 Shorts → 15 ±2; YTPROBE_IDLE 30s on one Short → count must not move; bubble reads 40 for 30 IG + 10 YT; one VRI construct for the session; IG ±2/50 unregressed. NEXT after acceptance: block screen + the UI/visual pass.
- 2026-07-25 — Phase 2 — Decision gate + Step 1 of the visible-product run. (0) GATE — YouTube took option (b), BETA, not (a): the three YTPROBE captures D31 was built for were never delivered, so there was no evidence to lock a strategy against, and YT is not allowed to block the phase. Added `Maturity {STABLE, BETA}` to PlatformSpec with block-eligibility DERIVED from it (`blocksAtLimit = blockEnabled && STABLE`); OverlayController now asks `blocksAtLimit`, never raw `blockEnabled`. YouTube + TikTok + Snapchat are all BETA (YT undercounts, Snapchat overcounts, none calibrated); Instagram alone is STABLE. Recessive "BETA" pill on the Today breakdown + Apps bars. Counting/gating/storage untouched. D32. (1) STEP 1 — guilt pack: `R.array.block_guilt_lines` (4 lines, `.random()`, repeated ~1 in 4) replaced by bundled `assets/guilt_pack.json` in remote-config shape — schema_version/revision/pack_id/locale, `lines[]` of {id, category, weight, text}, and a `surfaces` map holding the TONE POLICY (block = all 4 categories incl. pride per D9; bubble_cracking = roast/reverse_psych; bubble_fried = existential/roast) so voice-per-surface is remotely tunable, not hard-coded. 35 lines across roast/existential/reverse_psych/pride. `GuiltRotation` (pure, Random-injectable) = weighted draw with no repeat until the pool cycles, plus guards against a repeat at the cycle boundary and across surfaces. `GuiltPackParser` is total (unknown category / blank id / dup id / bad weight / unknown surface all dropped; only bad JSON, wrong schema, or zero lines reject) so a future server pack degrades gracefully on an old client. `GuiltPackLoader.install()` is the hot-swap seam with a revision guard. Block screen pulls from it; bubble nudges a line for ~4s on brain-state ESCALATION (50 🧠→🤯, 150 🤯→💀) then collapses to the count — D30's no-surface-churn property held (it's a resize, not a free/recreate). Added `testImplementation(org.json)` so a unit test parses the REAL bundled asset — a typo in the pack now fails the build instead of silently dropping every user to the 3-line fallback. D33. assembleDebug + testDebugUnitTest GREEN (54 tests; one real bug caught by the new tests — the cross-surface `avoid` guard was applied after the cycle filter and so lost in exactly the case it existed for). PENDING on-device: Step 1 acceptance (see HANDOFF). NEXT: Step 2 — blockEnabled=true for Instagram only + user-set daily limit + always-exitable block over clips_viewer.
- 2026-07-25 — Phase 2 — YT capture round, instrumentation only (D31). NO counting logic changed; Instagram untouched. Added DEBUG-only `YtProbe` (service/YtProbe.kt) emitting one `YTPROBE` line per YouTube event across all THREE types (SCROLLED / CONTENT_CHANGED / WINDOW_STATE) with: raw eventTime + dtMs, the full YTFIELDS set, contentChangeTypes, identity= (id:text — which node carries a per-Short fingerprint), the ACTUAL branch taken (enum threaded out of onScrolled, not re-derived: counted / debounced-quiet-gap / rejected-container / rejected-surface / probe-only), an entryCredit flag for D29's separate Room increment, and monotonic ytEventsSeen/ytCounted. TYPE_WINDOW_CONTENT_CHANGED is now observed for YT (marker-matched only, throttled identity scan) but still drives zero counting/surface work — it's there to test whether it, not TYPE_VIEW_SCROLLED, is YT's real per-Short signal. A `YTPROBE*` DIAG_LABEL broadcast zeroes the counters and prints the swipe/idle/tap decision table into the transcript. Deleted the superseded `DIAG YTFIELDS` dump from SurfaceDiagnostics. assembleDebug + testDebugUnitTest GREEN. STOPPED for the operator's three captures (YTPROBE_SWIPE / YTPROBE_IDLE / YTPROBE_TAP) — the numbers pick the strategy. NEXT after captures: implement the chosen YT advance strategy as a per-platform DetectionStrategy, then Phase 2 guilt pack + block screen on IG.
- 2026-07-24 — Phase 2 — Re-tour follow-ups, part 1 of 3 (churn + YT instrumentation). Re-tour confirmed: IG PASS (feed/profile/DMs NO_MATCH counted=false under ENFORCED; Reels MATCH(clips_viewer) counted=true). YT Shorts: container fix (D27) worked but EVERY Shorts event reports scrollDeltaY=0 → SAME → "debounced (SAME inside quiet-gap)", so only the landing short counts — Shorts needs its own advance signal. (1) CHURN (D30): the BLASTBufferQueue construct→destruct every ~3–5s was the bubble *surface* being freed/re-allocated by the View.GONE toggle each hysteresis cycle (WMS frees a GONE window's surface) — NOT addView/removeView. Fixed: bubble root stays View.VISIBLE for the service lifetime; hidden = alpha 0 + FLAG_NOT_TOUCHABLE (passes touches through), shown = alpha 1 + clear the flag, both via updateViewLayout so the single surface persists (OverlayController.setBubbleShown). (2) YT SIGNAL DISCOVERY (instrumentation only): added SurfaceDiagnostics.logScrollFields — a DEBUG dump of the AccessibilityEvent fields the compact line drops (fromIndex/toIndex/itemCount, scrollX/Y, maxScrollX/Y, scrollDeltaX/Y, source contentDescription/text), gated to YT-matched scrolls, emitted as `DIAG YTFIELDS …`. assembleDebug GREEN. STOPPED for the operator's ~15-Shorts capture (part 2). NEXT after capture: pick YT's advance discriminator from the dump (hypothesis: fromIndex/toIndex transition), add a per-platform advance strategy to PlatformSpec (DELTA_Y_FORWARD for IG, YT's proven signal), then the dashboard per-app breakdown (part 3).
- 2026-07-24 — Phase 2 — Surface markers LOCKED from the on-device tour (D26–D29). FIX 1: IG marker proven = "clips_viewer" (Reels srcId=clips_viewer_view_pager MATCH; feed/profile-grid/DMs/Stories all NO_MATCH) → IG flipped SHADOW→ENFORCED, blockEnabled still false. FIX 2: YouTube Shorts counted zero because its container is the legacy `android.support.v7.widget.RecyclerView` and the container check used fully-qualified AndroidX names — replaced with simple-class-name matching (PlatformSpec.matchesContainer), YT ENFORCED on proven "reel_recycler" (dropped the unverified shorts_* guesses — feed carries a Shorts shelf). FIX 3: overlay surface state now per-event (scrolled-node ancestry) + 3s hysteresis instead of a whole-window scan that false-positived on the YT feed's Shorts shelf; onForegroundChanged only clears on leaving a tracked app. FIX 4: bubble stays attach-once and is now surface-driven VISIBLE/GONE (deleted the per-scroll idle add path) — no VRI churn; plus entering a surface counts the landing reel once per session (3s guard). TikTok + Snapchat set to SHADOW (not toured; were ENFORCED-with-guesses). assembleDebug + testDebugUnitTest GREEN (added container-match + gating-mode tests). Pending on-device (Step 5): recalibrate ±2/50 under ENFORCED, confirm 0 counts on feed/profile/DMs, confirm YT Shorts count non-zero (or report if Shorts only emits deltaY=0), confirm no VRI churn. Re-tour commands handed off.
- 2026-07-24 — Phase 2 — Housekeeping + surface-tour harness prep (pre-tour). (0) Reconciled phase labels to reality: Phase 1 (Detection MVP) marked ✅ COMPLETE (calibrated in D11; repo scaffold + all detection boxes done), `← CURRENT` moved to Phase 2. Build confirmed green (assembleDebug + testDebugUnitTest); KSP2 did NOT bite on AGP 9.3.1 / KSP 2.3.2 / Room 2.7.1 — no `ksp.useKSP2=false` needed. (1) Rewrote SurfaceDiagnostics for a CLEAN one-line-per-event transcript: `DIAG SCROLL …` (pkg, dir, src class+id, SurfaceMatcher verdict MATCH(marker)/NO_MATCH, gating, counted, reason, full ancestor id-chain) and `DIAG WINDOW …` (per TYPE_WINDOW_STATE_CHANGED: tracked, gating, rootId, window verdict, src ancestry). Dropped the per-scroll hierarchy flood + the redundant non-DIAG SCROLL_DETECTED/IGNORED lines; WINDOW_CONTENT_CHANGED no longer logs. (2) Added a DEBUG-only exported broadcast receiver (`com.scrollkiller.DIAG_LABEL`, extra `label`) so the tour can stamp the log between steps via `adb shell am broadcast` — each stamp prints a banner + ONE bounded window-tree snapshot (so a non-scrolling surface still yields ids). Registered exported only in debug (shell UID), unregistered on unbind/destroy; compiled out of release. STOPPED for the on-device tour (Step 2). Next: paste transcript → derive+lock IG markers → IG SHADOW→ENFORCED (blockEnabled stays false) → recalibrate ±2/50; leave TikTok (and Snapchat if inconclusive) in SHADOW.
- 2026-07-22 — Phase 1 — Shipped debounced reel detection (activity-reset 200ms, forward-only) behind a PlatformSpec registry (Instagram enabled); emits `REEL_ADVANCE platform=…`. Added disclosure-first onboarding + deep-link to Settings + onResume re-check. Home shows a live in-memory count (TodayCounter). SwipeDetectorTest green; app compiles. Pending: on-device 50-swipe ±2 calibration; Room persistence + real day-boundary; brain-state visual.
- 2026-07-22 — Phase 1 — Detection calibrated on-device (49/50, safe undercount) — exit criterion MET (D11). Added Room persistence: daily_counts mirror + DAO (atomic upsert-increment), CountRepository (Room-backed, replaces TodayCounter), HomeViewModel; count now survives process death. Interim device-local day boundary (TODO Phase-3 server-truth). Added Room+KSP+viewmodel-compose deps. Recorded IG container finding (ViewPager vs RecyclerView, D15). Compiles (KSP validates SQL); SwipeDetectorTest green; DailyCountDaoTest added (instrumented — needs device). Pending: on-device force-stop persistence check; brain-state visual; Phase 3 sync.
- 2026-07-22 — Phase 1 — Fixed two device bugs + a capture gap (D17). (1) Re-added rich node-tree diagnostics permanently behind BuildConfig.DEBUG (SurfaceDiagnostics; `DIAG`-prefixed logs) so the surface tour can find IG's Reels discriminator. (2) Added surface gating: PlatformSpec.surfaceMarkers + SurfaceMatcher + a single onDoomSurface state driving BOTH counting and bubble visibility; ships as a no-op pass-through (empty markers) until the tour fills the discriminator — fallback = window-content heuristic only if ids prove obfuscated. (3) Fixed overlay churn: bubble now attached once (onServiceConnected) and toggled VISIBLE/GONE, killing the VRI construct/destroy cycles + repeated "displaying over other apps" notification. assembleDebug + testDebugUnitTest green. PENDING (next session, on-device): run the labeled surface tour (feed→reels→stories→profile→DMs→reels), paste the DIAG log, pick surfaceMarkers; then verify counting only on Reels + bubble only on Reels + no window churn, and re-run the ±2/50 calibration.
- 2026-07-22 — Phase 1 — Calibration Round 1 recorded: no new figure — already captured in D11's on-device run (slow 20/20, rapid 19/20, mixed 10/10). Shipped floating live-counter bubble (D16): OverlayController owns the SYSTEM_ALERT_WINDOW bubble (service stays event-only), reads the same observeToday() Flow as Home, updates on change only, collects only while visible, no-ops without overlay permission. Added optional overlay onboarding step (OverlayPermissionScreen + OverlayStatus) with 3-way MainActivity routing and a persisted skip flag. Broadened accessibility scope (removed packageNames, added typeWindowStateChanged) so the bubble hides on leaving a tracked app; updated disclosure text. Created HANDOFF.md manual test checklist (incl. comments-scroll false-count edge case). Pending on-device: bubble show/hide/drag/tap, count-matches-Home, works-without-permission, comments/IME edge cases.
- 2026-07-22 — Phase 1 — Shipped brain-state visual (D18): com.scrollkiller.brain.BrainState (pure Kotlin, one source of thresholds+arc: 🧠 0..49 / 🤯 50..149 / 💀 150+, green/amber/red). Home renders the brain hero + tinted count + label; overlay bubble shows emoji+count with per-state background tint (inside the existing distinctUntilChanged collector — no animation loop, CHEAP held). White bubble text for legibility over the tint. BrainStateTest (boundary table) + SwipeDetectorTest green; compileDebugKotlin SUCCESSFUL. Home @Preview covers all 3 states. Pending on-device: eyeball 🧠→🤯 flip at 50 on Home + bubble. Still blocked: surface-gating ADR needs the SurfaceDiagnostics resource-id (surfaceMarkers still empty).
- 2026-07-23 — Phase 2 — Surface discrimination + multi-app + polished UI + persistence (D20–D24). (1) DETECTION SURFACE GATING: added GatingMode {PASSTHROUGH/SHADOW/ENFORCED} so we could ENCODE candidate (device-unverified) Reels/Shorts markers without regressing IG's calibrated Phase-1 detection — IG runs SHADOW (evaluates + logs SCROLL_DETECTED/SCROLL_IGNORED/SHADOW_MISS, but still counts), new platforms run ENFORCED (count only on marker match → undercount-safe). Block escalation moved to a separate blockEnabled flag, false everywhere (D19's "never the feed" preserved + strengthened). (2) MULTI-APP: YouTube Shorts + TikTok (com.zhiliaoapp.musically, corrected from the spec's non-existent package) + Snapchat Spotlight scaffolded in PlatformRegistry (data-only; forPackage is the sole gate). (3) BUBBLE now shows only on active scroll and self-hides after 2s idle (service pokes overlay.onScrollActivity per counted advance; Handler-timed hide); Settings toggle can disable it. (4) PERSISTENCE: raw scroll_events table (dual-write w/ daily_counts, pruned >7d, DB v1→v2 migration) — realizes invariant #4/D4. (5) UI: replaced single-count Home with a Material3 bottom-nav dashboard (Today: brain + total + "~N mins" + per-app; Apps: single-accent bar chart; Settings: perms links + bubble toggle + clear-data). Deliberately kept com.scrollkiller + Compose (not the incoming spec's com.example + XML/Fragments), skipped QUERY_ALL_PACKAGES (registry displayNames) and the GENERIC_SCROLL fallback (would count all feeds). assembleDebug + testDebugUnitTest green (added PlatformRegistryTest, TimeEstimateTest). PENDING on-device: labeled SurfaceDiagnostics tour per platform to replace candidate markers, then flip IG→ENFORCED + set blockEnabled, re-run ±2/50 calibration; verify new platforms' hints/markers fire.
- 2026-07-22 — Phase 2 — Shipped the block screen (D19): full-screen overlay over the reel surface at PlatformSpec.dailyLimit (default 100), with Exit (→ launcher), Unlock (temporary bypass — challenge next), and Back=Exit. Pure BlockPolicy.overlayFor gates bubble-vs-block; SAFETY GATE keeps it dormant until surface gating is real (surfaceMarkers non-empty) so it never covers the feed. Service doom-surface signal now carries the Platform (onSurface/offSurface); OverlayController is the coordinator owning bubble + new BlockScreenController; added per-platform count (DAO observeCountForDatePlatform + CountRepository.observeToday(platform)). New BlockScreenController (focusable full-screen, add/removeView, no-op without overlay permission) + overlay_block.xml + guilt string-array. BlockPolicyTest + BrainStateTest + SwipeDetectorTest green; compileDebugKotlin SUCCESSFUL. Play-policy posture recorded in D19. PENDING on-device: cannot live-demo the block until the surface tour fills surfaceMarkers (safety gate keeps it dormant); then verify block only on Reels, Exit/Back leave, re-show on foregrounding. NEXT checkbox: the actual challenge (replaces Unlock bypass).
